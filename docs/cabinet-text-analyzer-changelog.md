# Анализ текста — журнал версий страницы

Badge в шапке карточки: **vX.Y** или **vX.Ys** (stable). Конфиг: `config/cabinet-text-analyzer.php`.  
При багрепорте: «на v1.2 работало, на v1.4 — нет».

## 6.9s — 2026-05-24 (stable)

- **Зафиксирована стабильная база:** PDF (логотип `logo-icon-pdf.png`, поля, график Zipf), обложка, экспорт.
- **Эталон для других модулей:** [cabinet-pdf-report-template.md](./cabinet-pdf-report-template.md) — layout, запреты, чеклист копирования.
- Следующие эксперименты — версия **7.0** или **6.9s-dev**, не правки поверх stable без bump.

## 6.9 — 2026-05-24

- **PDF обложка:** эталонный `logo-icon-pdf.png` от дизайна; кэш обложки сбрасывается по mtime иконки.

## 6.8 — 2026-05-24

- **logo-icon-pdf.png:** снова растр из `logo-icon.svg` (qlmanage/rsvg), белый фон → прозрачность; GD-заглушка только fallback.

## 6.7 — 2026-05-24

- **PDF логотип:** иконка — GD-PNG на прозрачном фоне (без qlmanage); блок «иконка + Датагон» собирается отдельным PNG и вставляется на обложку; verify проверяет прозрачность углов.

## 6.6 — 2026-05-24

- **PDF обложка:** логотип как на datagon.ru — прозрачная иконка + «Датагон»; убран белый прямоугольник от растра `logo.svg`; автопроверка `verifyCoverPng`.

## 6.5 — 2026-05-24

- **PDF обложка:** логотип целиком из `logo.svg` (иконка + «Датагон»), как в сайдбаре и на datagon.ru — без самодельной сборки и слогана.

## 6.4 — 2026-05-24

- **PDF:** прозрачный логотип на обложке; поля 14 mm на страницах отчёта; график Zipf — UTF-8 подписи (DejaVu), без растягивания.

## 6.3 — 2026-05-24

- **PDF обложка:** реальный логотип из `logo-icon.svg`; убран CTA-блок; контент вертикально по центру листа.

## 6.2 — 2026-05-23

- **PDF обложка:** CTA-блок в середине листа; вставка через `Image()` 210×297 (не `<img>` — в Preview футер «плавал»); сброс кэша cover PNG.

## 6.1 — 2026-05-23

- **PDF обложка:** вставка через `<img>` (не `mPDF::Image` + `AddPage` — первая страница была пустой).
- **PDF тело:** симметричные поля 14 мм, обёртка `.report-body`.
- **Проверка:** `scripts/verify-text-analyzer-pdf.php` — обложка с картинкой, текст со 2-й страницы.

## 6.0 — 2026-05-23

- **PDF обложка:** рендер целиком в один PNG (GD + DejaVu) — полный A4 без белой полосы и без HTML/mPDF layout.

## 5.9 — 2026-05-23

- **PDF обложка:** фон через CSS `background-image` в HTML вместо `mPDF::Image` — иначе текст уходил под фон или на y=0 и не был виден.

## 5.8 — 2026-05-23

- **PDF обложка:** подложка через `mPDF::Image` на весь лист 210×297 мм (убрана белая половина страницы); логотип без прозрачного ореола (фон `#1a3a8f`).

## 5.7 — 2026-05-23

- **PDF:** обложка — подложка как на [analiz-teksta](http://localhost:3001/analiz-teksta/) (синий градиент + сетка), логотип иконка+«Датагон»; страницы 2+ — **белый фон** (убран затекший `@page` с обложки).

## 5.6 — 2026-05-23

- **PDF обложка:** логотип без PNG (синий блок + «D», без белых уголков); фон `#0f172a` на весь A4 (`@page margin 0`, поля 0); нижний колонтитул на тёмном фоне. Иконка в теле отчёта — только GD с прозрачностью.

## 5.5 — 2026-05-23

- **Публичная ссылка не слетает при новом анализе** — снимок в БД; отзыв только по кнопке «Отозвать» или при «Обновить» для **того же** отчёта. Раньше каждый analyze() отзывал все ссылки → 404.

**Проверить:** создать ссылку → новый анализ → старая ссылка в инкогнито **200**.

## 5.4 — 2026-05-23

- **Публичная ссылка — логотип:** в шапке иконка `logo-icon.svg` + «Датагон» текстом HTML (не SVG-текст); крупнее и читаемо на белом.

## 5.3 — 2026-05-23

- **PDF обложка:** без белого квадрата логотипа (иконка PNG + текст «Датагон»), тёмный фон на **весь лист A4** (поля 0 на 1-й странице).

**Проверить:** badge **v5.3**, PDF → обложка целиком тёмная, логотип без белой подложки.

## 5.2 — 2026-05-23

- **Публичная ссылка** (`/public/share/text-analyzer/…`) — шапка на белом с логотипом `logo-on-light.svg`, кнопки «Войти» / «Зарегистрироваться бесплатно», блок CTA с оффером регистрации (`?module=text-analyzer&from=text-analyzer-public-share`).

**Проверить:** открыть публичную ссылку в инкогнито — читаемый «Датагон», CTA регистрации.

## 5.1 — 2026-05-23

- **Панель «Выгрузка и публичная ссылка»** — две карточки (экспорт / шаринг), единый стиль, бейдж срока ссылки, кнопка «Скопировать» с откликом.

**Проверить:** badge **v5.1**, после анализа — аккуратная панель над результатами.

## 5.0 — 2026-05-23

- **Сравнение с конкурентом** — подпись «Конкурент · `domain.ru`» в бейджах, заголовках таблиц, легенде графика Ципфа и PDF (вместо голого «Конкурент» / полного URL).

**Проверить:** badge **v5.0**, анализ с URL конкурента → в таблицах «Конкурент · example.com».

## 4.9 — 2026-05-23

- **Ципф — идеальные значения:** под графиком таблица (#, слово, реальные, идеальные, Δ); на графике линия идеала **всегда** (и в сравнении с конкурентом — 3 линии). PDF: колонка «Идеальные», оранжевая линия на графике.

**Проверить:** badge **v4.9**, один URL без конкурента → таблица с «Идеальные значения» и пунктир на графике.

## 4.8 — 2026-05-23

- **PDF — визуальный редизайн:** тёмная обложка с **настоящим логотипом из SVG** (`qlmanage` → PNG), спокойная типографика, светлые заголовки таблиц (не «Excel-синий»), график Ципфа как **картинка**, облака — **таблица топ-слов**, а не «таблетки»; разрыв страницы перед длинными таблицами.
- Логотипы: `public/img/logo-cover-pdf.png`, `logo-icon-pdf.png` из `logo.svg` / `logo-icon.svg`.

**Проверить:** badge **v4.8**, PDF → обложка slate + логотип «Датагон», внутри график и читаемые таблицы.

## 4.7 — 2026-05-23

- **PDF переработан** — прямой **mPDF** (без Excel/HTML-конвертера): полноценная обложка, колонтитулы с логотипом, KPI-карточки, секции, сравнение с конкурентом в PDF.
- Сервис: `App\Services\TextAnalyzerPdfService`, шаблоны `export/pdf-cover.blade.php`, `export/pdf-body.blade.php`.

**Проверить:** badge **v4.7**, «Скачать PDF» — обложка «Датагон», не «таблица в Word».

## 4.6 — 2026-05-23

- **Сравнение с конкурентом** — переключатель «Сравнить с конкурентом» → поле URL конкурента; второй анализ страницы; KPI, график Ципфа (ваша / конкурент), облака попарно по зонам, таблицы слов и биграмм с колонками «ваша / конкурент / Δ».

**Проверить:** badge **v4.6**, URL своей страницы + URL конкурента → таблицы с Δ, два облака в каждой зоне.

## 4.5 — 2026-05-23

- **Публичная ссылка на отчёт** — блок в export-bar на `/text-analyzer`: создать / скопировать / отозвать (30 дней, одна активная ссылка на пользователя).
- Просмотр без входа: `GET /public/share/text-analyzer/{token}` — те же KPI, Ципф, облака, таблицы (`layouts/public-module`).
- Таблица `text_analyzer_public_shares`, cron очистки; с v5.5 новый анализ **не** отзывает старые ссылки.

**Проверить:** badge **v4.5**, после анализа → «Создать публичную ссылку» → URL в инкогнито **200**; `bash scripts/smoke-text-analyzer.sh` → `public_share: OK`.

## 4.4 — 2026-05-23

- **PDF-отчёт переработан** — шапка с логотипом **Датагон** (`logo-pdf.png` / иконка), брендовая полоса, KPI, настройки с бейджами, таблица Ципфа (факт / идеал / Δ), облака слов (топ по зонам), таблицы слов и фраз; колонтитул с логотипом и нумерацией страниц.
- Генерация PNG логотипа при первом экспорте: `App\Support\TextAnalyzerPdfBranding`.

**Проверить:** badge **v4.4**, PDF → на первой странице логотип и «Датагон», не «голый» синий заголовок без бренда.

## 4.3 — 2026-05-23

- **«Исключать союзы, предлоги, местоимения» включено по умолчанию** — toggle зелёный на чистой форме; снятие сохраняется (hidden `0` + checkbox `1`).

**Проверить:** badge **v4.3**, `/text-analyzer` без анализа → switch вкл; снять → анализ → после F5 switch выкл.

## 4.2 — 2026-05-23

- **Выгрузка Excel** — 3 листа: сводка, слова, словосочетания (`Maatwebsite/Excel`).
- **PDF-отчёт** — первая версия (KPI, настройки, топ слов/фраз) через mPDF; брендинг доработан в **v4.4**.
- Кнопки «Скачать Excel» / «Скачать PDF» над результатами; snapshot в session до нового анализа.

**Проверить:** badge **v4.2**, после анализа — выгрузка xlsx и pdf без повторного POST формы.

## 4.1 — 2026-05-23

- **Порядок блоков:** «Облака» перед «Общий анализ слов» (после графика Ципфа).

**Проверить:** badge **v4.1**, облака выше таблицы слов.

## 4.0 — 2026-05-23

- **KPI пересчитываются при исключении слов** — «Количество слов/символов/пробелов» считались из исходного текста, игнорируя `removeWords`; теперь из того же `$total + $link`, что таблица и облака.

**Проверить:** badge **v4.0**, исключить `персональных` → KPI меньше, слова нет в таблице.

## 3.9 — 2026-05-23

- **KPI-карточки ровно по центру** — иконка на всю высоту блока (`align-self: stretch`), текст `justify-content: center`, одинаковые padding; убрана фиксированная высота 56px (из‑за неё «символы без пробелов» уезжали вверх).

**Проверить:** badge **v3.9**, четыре KPI одной высоты, иконка и цифры по центру вертикали.

## 3.8 — 2026-05-23

- **Отступы в блоке облаков** — убран `padding: … 0` у `.cabinet-ta-cloud-panel` (прилипало к краям карточки); padding у зон и внутри области облака; `edgePad` 18px в спирали — слова не впритык к рамке.

**Проверить:** badge **v3.8**, «Зона текста» и облако с воздухом от краёв карточки и рамки.

## 3.7 — 2026-05-23

- **Один tooltip на слово в облаке** — убран CSS `::after` (дублировал JS-подсказку «слово — Повторений: N»).

**Проверить:** badge **v3.7**, hover на слово → одна подсказка у курсора.

## 3.6 — 2026-05-23

- **Toggle ровно в колонку** — обёртка `cabinet-ta-switch-row__toggle` (1.75rem), flex-строки вместо `display: contents`; track switch без отрицательных отступов; кнопка «Анализировать» выровнена с подписями.

**Проверить:** badge **v3.6**, все toggle в одной вертикали, текст на одной линии, switch по центру строки.

## 3.5 — 2026-05-23

- **Облака снова спиральные** (без jQCloud) — слова «комком» по центру, размер и цвет по частоте, hover → число повторений.
- **Вёрстка блока облаков:** шапка карточки «Облака», отступы у подсказки и зон; высота области **400px** (+25%), рамка у каждой зоны.

**Проверить:** badge **v3.5**, три облака плотные (не строка по центру), подписи не прилипают к краю карточки.

## 3.4 — 2026-05-23

- **Toggle в одну колонку** — CSS Grid + `display: contents` на строках, фиксированная ширина switch (2.25rem); убран «скачок» и огромный зазор между toggle и подписью.
- Опечатка в переводе: `tittle` → `title`.

**Проверить:** badge **v3.4**, все четыре toggle строго в одной вертикали, подписи сразу справа.

## 3.3 — 2026-05-23

- **Компактные переключатели** — убраны лишние обёртки (`switch`, `__helper-link`, `mb-2`), строки `cabinet-ta-switch-row`; в CSS убраны `width/height` на label и `display: block` на switch внутри формы (убирали «полкилометровые» отступы между toggle).

**Проверить:** badge **v3.3**, четыре toggle плотно друг под другом (~0.35rem), без огромного зазора между textarea и первым switch.

## 3.2 — 2026-05-23

- **Переключатели red/green** — в `cabinet-bs4-compat.css` стили AdminLTE 3 `custom-switch-off-danger` / `custom-switch-on-success` (на layout AdminLTE 4 без них были обычные чекбоксы).

**Проверить:** badge **v3.2**, четыре toggle: выкл — красный, вкл — зелёный.

## 3.1 — 2026-05-23

- **jQCloud убран полностью** — он блокировал main thread (страница не кликается, одно слово «на», пустые таблицы). Облака — быстрый tag-cloud, три зоны сразу, без зависаний.
- Chart.js без анимации; init без лишних setTimeout.

**Проверить:** badge **v3.1**, все три облака заполнены словами, биграммы в таблице, страница кликабельна.

## 3.0 — 2026-05-23

- **Исключение из биграмм:** слово/фраза из списка убирается из «Словосочетания из 2 слов» (раньше оставались «персональных данных», «обработку персональных» и т.п.).
- Список исключений — **одна строка = одно слово или фраза** (как в lk.redbox.su), не разбивается по пробелам.

**Проверить:** badge **v3.0**, исключить `персональных` → в биграммах нет фраз с этим словом.

## 2.9 — 2026-05-23

- **Исключение своих слов снова работает:** форма шлёт `removeWords=1`, PHP ждал `'on'`; плюс `strtolower()` ломал кириллицу — заменено на `mb_strtolower` и regex по словам.
- Исключённые слова убираются из текста, таблицы, облаков и графика Ципфа.
- Три облака рисуются **по очереди** (не параллельно) — меньше шанс зависания UI.

**Проверить:** badge **v2.9**, исключить «персональных» → слова нет в таблице/облаке/графике, страница кликабельна.

## 2.8 — 2026-05-23

- График Ципфа: подпись оси X **«Плотность слова»** вместо «Ранг слова» (как в таблице слов).

## 2.7 — 2026-05-23

- **Табы облака убраны** — три зоны (текст / ссылки / обе) видны сразу: сетка 3 колонки (XL), 2+1 (MD), столбец (SM).
- Все облака рисуются при загрузке (jQCloud с небольшим сдвигом по времени, без переключения вкладок).

**Проверить:** badge **v2.7**, три облака на одном экране с подписями зон.

## 2.6 — 2026-05-23

- Переключатели как на **Redbox / анализе релевантности**: зелёный вкл / красный выкл (`custom-switch-off-danger custom-switch-on-success`).
- Облако: **jQCloud спираль** (как старый LK), веса 1–100 для контраста размеров; fallback — только tag-cloud.
- Tooltip при наведении на слово (число повторений); контейнер облака 350px без рамки.

**Проверить:** badge **v2.6**, toggles зел/крас, облако «комком», hover → «Повторений: N».

## 2.5 — 2026-05-23

- Облако снова **jQCloud** (спираль, классы w1–w10, палитра как в старой версии / анализе релевантности).
- Режим: `shape: false`, `delayedMode: true`, `removeOverflowing: false` — без зависания rectangular.
- Fallback: свой spiral-рендер с теми же классами jqcloud.

**Проверить:** badge **v2.5**, облако плотное «комком», hover → «Повторений: N».

## 2.4 — 2026-05-23

- Лимит облака **100** слов (бэкенд + фронт); высота блока 420px; шрифт чуть меньше при >45 словах.

**Проверить:** badge **v2.4**, подпись «до 100», облако плотнее но без зависания.

## 2.3 — 2026-05-23

- График Zipf: **синяя сплошная** (факт) vs **оранжевая пунктирная** (идеал) — линии не сливаются.

**Проверить:** badge **v2.3**, две линии на графике различимы по цвету и типу.

## 2.2 — 2026-05-23

- Облако: **collision detection** (спираль + проверка bbox) — слова не накладываются.
- Подсказка при наведении: `data-tip` → «слово — Повторений: N» (CSS tooltip).
- Лимит облака **35** слов; длинные слова чуть меньше по размеру.

**Проверить:** badge **v2.2**, hover на слово → число повторений, без «стопки» в центре.

## 2.1 — 2026-05-23

- **jQCloud убран** — блокировал главный поток (rectangular + collision loop → зависание после 1-го слова).
- Облако: лёгкая **спиральная** отрисовка на чистом JS/CSS (O(n), без плагина).
- Watchdog `releaseUiLock` каждые 250 ms (6 с); таблицы инициализируются до графика.

**Проверить:** badge **v2.1**, dealmed → ~45 слов в облаке, страница кликабельна, F5 работает.

## 2.0 — 2026-05-23

- **DataTables полностью убран** — таблицы слов и фраз: нативный HTML + прокрутка (`max-height: 28rem`) + поле поиска.
- Словоформы: раскрытие через соседний `<tr>` (без `row.child()` и без пустых полосок tbody).
- Со страницы сняты CSS/JS DataTables — меньше блокировок UI и крашей `_DT_CellIndex`.

**Проверить:** badge **v2.0**, dealmed URL → все строки слов видны в скролле, облако, клики по табам и кнопкам.

## 1.9 — 2026-05-23

- jQCloud: **45 слов**, `shape: rectangular`, `delayedMode: true` (не блокирует UI), один paint на вкладку.
- DataTables — **deferRender** + старт через 900 ms после облака (страница кликабельна сразу).
- Страховка: `releaseUiLock` через 4 с после загрузки.

**Проверить:** badge **v1.9**, dealmed → облако с десятками слов, клики работают.

## 1.8 — 2026-05-23

- Облако снова **jQCloud** (спираль, размер по частоте — как на анализе релевантности), fallback — строка тегов если плагин недоступен.
- Без ResizeObserver и без drawCallback DataTables (зависания из v1.5–1.6 не возвращаем).

**Проверить:** badge **v1.8**, dealmed URL → облако «комком» в центре, не строка.

## 1.7.1 — 2026-05-23

- Убран `cursor: help` (курсор «?») у таблицы фраз и облака тегов — обычная стрелка.

## 1.7 — 2026-05-23

- **Заморозка:** убран `drawCallback` DataTables → `child.hide()` (бесконечный цикл draw).
- **Облако:** jQCloud заменён на простые HTML-теги (`.cabinet-ta-tag-cloud`) — без плагина, без зависимости от размеров контейнера.
- jQCloud отключён на странице (`index.blade.php`).

**Проверить:** badge **v1.7** → URL или dealmed → облако с словами, таблица с строками, клики работают.

## 1.6 — 2026-05-23

- **Заморозка вкладки:** убран ResizeObserver у jQCloud (бесконечный цикл paint→resize→paint).
- **Session:** результат снова flash + сброс после показа — F5 и новые вкладки без «залипшего» dealmed.
- Init таблиц отложен (setTimeout), принудительный сброс modal-backdrop / pointer-events.

**Проверить:** badge **v1.6** → анализ URL → страница кликабельна; F5 — пустая форма.

## 1.5 — 2026-05-23

- Клиентская логика вынесена в `public/js/cabinet-text-analyzer.js` (отдельный модуль вместо 500+ строк inline).
- DataTables **core-only** (без responsive/buttons — они могли ломать клики).
- jQCloud: `removeOverflowing: false`, `shape: rectangular`, ResizeObserver + IntersectionObserver.
- Убран `contain: paint` у облака; canvas Chart.js ограничен высотой контейнера.
- Результаты анализа сохраняются в session (не flash) — F5 не сбрасывает таблицы.

**Проверить:** badge **v1.5**, URL dealmed → облако ~80 слов, пагинация 1–25 → 26–50, клики по табам.

## 1.4 — 2026-05-23

- Word forms в JSON (`#cabinet-ta-word-forms`), не 200+ скрытых `<div>` в DOM (страница после URL ~1.1 MB → легче).
- jQCloud: `removeOverflowing`, `delayedMode: false`, повторный unlock UI после init.
- Глобальный `cabinetReleaseUiLock`, убрана кнопка maximize у карточки модуля.

**Проверить:** http://localhost:3002/text-analyzer → URL `https://www.dealmed.ru/o_kompanii.html` → пагинация, табы облака, переключатели режима, «Анализировать». Badge **v1.4**.

## 1.3 — 2026-05-23

- DataTables `row.child()` для словоформ (без `colspan` в tbody).
- Пагинация 25/50/100/250; smoke `scripts/smoke-text-analyzer.sh`.
- Первый `cabinetTaReleasePageLock` (modal-backdrop / maximized-card).

## 1.2 — 2026-05-23

- Исправлен краш DataTables на detail-rows с `colspan=5`.
- Облако инициализируется после табов.

## 1.1 — 2026-05-23

- Базовая фиксация версий; редизайн AdminLTE 4 / BS5, DataTables + jQCloud.
