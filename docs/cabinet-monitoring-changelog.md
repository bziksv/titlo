# Кабинет — мониторинг позиций (changelog)

**Модуль:** `/monitoring`, UI v2: `/monitoring-v2`  
**Config:** `cabinet.datagon.ru/config/cabinet-monitoring.php`  
**Бенчмарк UX:** [cabinet-monitoring-benchmark-seranking.md](./cabinet-monitoring-benchmark-seranking.md), [cabinet-monitoring-benchmark-topvisor.md](./cabinet-monitoring-benchmark-topvisor.md), [cabinet-monitoring-benchmark-comparison.md](./cabinet-monitoring-benchmark-comparison.md)

## 3.5.151-dev — 2026-05-31

- **`/monitoring/top-100/{id}`:** placeholder «Выберите фразу» в select2 больше не съезжает вниз — переопределён `line-height` bootstrap4-theme для sm-поля.
- **Проверка:** Ctrl+F5 → текст «Выберите фразу» по центру, в одну линию с периодом и регионом.

## 3.5.150-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** форма шагов 1–4 при открытии **пустая** — фраза, период и регион не подставляются сами (раньше это были дефолты приложения, не автозаполнение браузера). Период заполняется только после «Применить» в календаре; регион — только из URL `?region=` или ручной выбор.
- **Проверка:** Ctrl+F5 → «Выберите фразу», пустой период, «Выберите регион поиска»; шаги 1–3 в гайде серые до выбора.

## 3.5.149-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** убран «хвост» у select2 фразы — скрытый `<select>` без рамки; стрелка dropdown не выступает за нижний край; колонки шагов 1–4 и 5 на одной сетке `--top100-grid-cols`.
- **Проверка:** Ctrl+F5 → поле «Фраза» без полоски в правом нижнем углу; вертикали TOP/URL/фильтра совпадают с шагами 1–4.

## 3.5.148-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** убран пункт «Все поисковые системы» — одна доска на выбранную ПС; отключено автозаполнение браузера и поиск в select2 фразы.
- **Проверка:** Ctrl+F5 → регион — только конкретные ПС; анализ → одна доска без бейджа «все ПС».

## 3.5.147-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** форма выровнена через CSS subgrid — одна линия для полей и кнопки, общая строка подсказок, шаг 5 label/control в две строки; кнопка «Анализировать» не обрезается.
- **Проверка:** Ctrl+F5 → шаги 1–4: инпуты и кнопка на одной линии; шаг 5: подписи и контролы ровно.

## 3.5.146-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** шаги в «Как пользоваться» становятся зелёными после корректного действия (фраза, даты, регион, запуск, инструменты).
- **Проверка:** Ctrl+F5 → по шагам 1–4 → зелёные бейджи; после загрузки — шаг 5 при смене TOP/фильтра/подсветки.

## 3.5.145-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** выравнивание формы — кнопка «Анализировать» в одну линию с полями, подписи шага 5 на одном уровне, бейджи шагов по центру с текстом.
- **Проверка:** Ctrl+F5 → блоки «Шаги 1–4» и «Шаг 5» — поля и подписи без смещения по вертикали.

## 3.5.144-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** иконки Bootstrap Icons в контекстном меню строки (сайт, анализ, копирование, связи, удаление связи).
- **Проверка:** Ctrl+F5 → анализ → ⋮ у строки → иконки слева от пунктов.

## 3.5.143-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** пошаговый гайд (1–5), панели «Запуск» / «Настройка отображения» / «Результаты»; отступы колонок и строк; зазор между датами для линий связей.
- **Проверка:** Ctrl+F5 → шаги 1–4 → анализ → шаг 5 разблокируется.

## 3.5.142-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** «Все поисковые системы» — отдельная доска на каждую ПС (не пустой экран); колонки при любом числе результатов (не только 100); пустые даты — заглушка «Нет снимка»; экран «Нет данных» с пояснением.
- **Проверка:** фраза + «Все ПС» + период → блоки по Яндекс/Google; одна ПС → одна доска, связи между датами.

## 3.5.141-dev — 2026-05-30

- **`/monitoring/top-100/{id}`:** toolbar — flex вместо grid, убран `overflow:hidden` (кнопка «Анализировать» не обрезается), выравнивание по label, select2 `bootstrap4`.
- **Проверка:** http://localhost:3002/monitoring/top-100/509 — Ctrl+F5, кнопка «Анализировать» целиком справа в первой строке.

## 3.5.140-dev — 2026-05-30

- **`/monitoring/offset-positions`:** layout как set-positions — гайд, таблица логики, KPI, правила коррекции на странице (3 диапазона), кнопка «Экспорт с коррекцией» → модалка отчёта; правила передаются в GET `/monitoring/{id}/export`.
- JS: `cabinet-monitoring-offset-positions.js`; список проектов — все (как set-positions).
- **Проверка:** http://localhost:3002/monitoring/offset-positions — проект → правила → экспорт → скачивание PDF/Excel с скорректированными позициями.

## 3.5.139-dev — 2026-05-30

- **`/monitoring/set-positions`:** layout как admin/permissions — гайд (назначение, предупреждение, шаги), таблица логики подстановки, KPI, форма + журнал CodeMirror; явная кнопка «Запустить» с confirm и `cabinet-button-busy`; исправлен ответ `insertPositions` (JSON вместо перезагрузки списка ПС).
- JS: `cabinet-monitoring-set-positions.js`.
- **Проверка:** http://localhost:3002/monitoring/set-positions — проект → ПС → даты → «Запустить».

## 3.5.138-dev — 2026-05-30

- **`/monitoring/permissions`:** страница прав в проекте — layout как у admin/stat, справка «кто есть кто», таблица ролей, аккордеон по группам (проект, команда, ключи, съём, бюджет), подпись к каждому праву; автосохранение с блокировкой переключателей.
- Справочник: `App\Support\MonitoringPermissionsCatalog`.
- **Проверка:** http://localhost:3002/monitoring/permissions — Ctrl+F5, переключить галочку → toast «Права сохранены».

## 3.5.137-dev — 2026-05-30

- **`/monitoring/stat`:** полный перевод на русский (сводка, очередь, DataTables, приоритеты); оформление как у `/monitoring/admin`.
- **Проверка:** http://localhost:3002/monitoring/stat — Ctrl+F5.

## 3.5.136-dev — 2026-05-30

- **`/monitoring/admin`:** развёрнутая подсказка к «SERP (search_indices)» — что хранится, зачем, cron, чем отличается от dynamics и Free-позиций.
- **Проверка:** http://localhost:3002/monitoring/admin → блок «Очистка базы данных».

## 3.5.135-dev — 2026-05-30

- **Fix:** 500 на списке проектов — `_btn` больше не вызывает `admin-card` без `$description`; навигация админки — только pills.
- **Проверка:** http://localhost:3002/monitoring-v2 и `/monitoring` — без ViewException.

## 3.5.134-dev — 2026-05-30

- **Fix:** `/monitoring/admin` — убран мусор `");"="">` в полях (сломанный `onclick` с `@json` в кавычках).
- **Проверка:** http://localhost:3002/monitoring/admin — Ctrl+F5, поля без артефактов.

## 3.5.133-dev — 2026-05-30

- **`/monitoring/admin`:** страница настроек в стиле модуля — навигация pills, блоки с пояснениями «для воробьёв», одна кнопка «Сохранить».
- **Автоочистка отчётов «Динамика конкурентов»:** `competitors_changes_dates_retention_days` (по умолчанию **180**; **0** — не удалять). Cron 04:25 + `php artisan monitoring:prune-competitors-dynamics` (`--dry-run`, `--days=`). Удаляются `monitoring_changes_date` в статусе ready/fail старше N дней; очередь и расчёт не трогаются.
- **Проверка:** http://localhost:3002/monitoring/admin → блок «Очистка базы данных» → 180 → Сохранить; `php artisan monitoring:prune-competitors-dynamics --dry-run`.

## 3.5.132-dev — 2026-05-30

- **`onFreeTariff()`:** Free только если **нет** платной роли (Optimal / Ultimate / Maximum). Free+Ultimate → платный; cron автосъёма и retention позиций не блокируются. Согласовано с `getTariffByUser`.
- **Проверка:** creator с ролью Ultimate без pays — `monitoring:prune-free-positions --dry-run` не включает его проекты; auto_update разрешён при включённом расписании.

## 3.5.156-dev — 2026-06-01

- **Fix экспорт XLS/PDF мониторинга:** `PositionsExport::formatPosition` — `array_filter` без callback удалял позицию **`0`** (PHP считает `"0"` пустым) → `Undefined offset: 0` при `/monitoring/{id}/export`.
- **Проверка:** http://localhost:3002/monitoring/521/export?…&format=xls — проект с ячейками «0» в ТОП (geely-pragmatika.ru, май 2026).

## 3.5.155-dev — 2026-06-01

- **`monitoring:resume-auto-schedules`:** `--email=` / `--user=` — включить `auto_update` у **всех** регионов проектов владельца; `--only-with-schedule` — только с сохранённым расписанием (как раньше глобально).
- **Проверка:** `php artisan monitoring:resume-auto-schedules --email=sv6@list.ru` → все регионы пользователя с `auto_update=1`.

## 3.5.154-dev — 2026-06-01

- **Зависшие расписания** на **`/monitoring/admin`** и **`/users`**: общий partial `partials/monitoring-stale-schedules`, JS `cabinet-monitoring-stale-schedules.js`.
- **Проверка:** http://localhost:3002/users и http://localhost:3002/monitoring/admin.

## 3.5.153-dev — 2026-06-01

- **Зависшие расписания** на `/monitoring/admin` (якорь `#stale-schedules`). Порог: `stale_schedules_inactive_days` в `config/cabinet-monitoring.php`.
- **Логика:** `auto_update = 1` + неактивность N дней; отключение только по кнопке админа.

## 3.5.152-dev — 2026-06-01

- **Возобновление auto_update:** `php artisan monitoring:resume-auto-schedules` (платные владельцы, регион с сохранённым расписанием); `--project=ID`; `--dry-run`. Пара к `monitoring:suspend-auto-schedules`.
- **Проверка:** после глобального suspend — `monitoring:resume-auto-schedules`; на lk supervisor + `schedule:run`; на следующий слот расписания — новые строки в `search_indices`.

## 3.5.131-dev — 2026-05-30

- **Free: retention истории позиций:** `/monitoring/admin` — блок «Free: хранение истории позиций» (`free_tariff_positions_retention_days`, по умолчанию **365**; **0** — не удалять). Cron ежедневно 04:20 + `php artisan monitoring:prune-free-positions` (`--dry-run`, `--days=`). Удаляются строки `monitoring_positions` старше N дней у проектов с владельцем Free; затем пересчёт снимка списка. **search_indices** (SERP) — общий пул, не режется по тарифу.
- **Проверка:** http://localhost:3002/monitoring/admin → 365 → Save; `php artisan monitoring:prune-free-positions --dry-run`.

## 3.5.130-dev — 2026-05-30

- **Free — только ручной съём:** `auto_update` не ставится при сохранении расписания; cron и `AutoUpdateMonitoringPositions` пропускают владельцев с ролью Free; при входе в модуль — `enforceForFreeUser`.
- **Массовая приостановка:** `php artisan monitoring:suspend-auto-schedules` (все) или `--free-only`.
- **Модалка на страницах `/monitoring*`:** Free + сохранённое расписание → предупреждение с отложением на 2 нед.; очередь модалок с Telegram (`cabinet-modal-queue.js`, приоритет мониторинга выше).
- **Проверка:** Free-пользователь с расписанием → http://localhost:3002/monitoring-v2 — модалка; `php artisan monitoring:suspend-auto-schedules` — счётчик обновлённых регионов; мастер создания — шаг «Расписание» без формы, только подсказка про ручной съём.

## 3.5.129-dev — 2026-05-30

- **Список v2 — паритет с классикой для админа:** в навигации ссылка **«Корректировать»** (`/monitoring/offset-positions`); внизу страницы — блок **Administration panel** (как на `/monitoring`); столбцы **ТОП 5**, **Освоено**, в **Бюджет** — зелёный % освоенности (как в классической таблице).
- Настройки столбцов: ключ `mastered` в `monitoring_v2_user_preferences` (по умолчанию включён).
- **Проверка:** http://localhost:3002/monitoring-v2 под Super Admin / admin — F5 → ТОП 3/5/10/30/100, Бюджет с %, Освоено, панель админа внизу, «Корректировать» в nav.

## 3.5.128-dev — 2026-05-30

- **Динамика конкурентов (результат отчёта):** по умолчанию метрика **ТОП‑10** (график и таблица), не средняя позиция.
- **Проверка:** `/monitoring/competitors/result-analyse/{id}` — при открытии активна «Топ 10», ось Y «Доля в ТОП, %».

## 3.5.127-dev — 2026-05-30

- **Динамика конкурентов:** при удалении отчёта кнопка «Удалить» показывает спиннер «Удаление…», блокируется вместе с «Отмена» и × до ответа сервера.
- Общий хелпер `public/js/cabinet-button-busy.js`; правило агента `redbox-cabinet-button-busy.mdc`.
- **Проверка:** `/monitoring/{id}/competitors/dynamics` → корзина у готового отчёта → «Удалить» — сразу виден спиннер, повторный клик невозможен.

## 3.5.126-dev — 2026-05-29

- **Динамика конкурентов:** множественный выбор конкурентов в форме «Собрать отчёт» (Select2); ваш сайт всегда в отчёте. В таблице «Готовые отчёты» — подпись набора доменов; дубликат проверяется по периоду, региону и выборке.
- **Проверка:** `/monitoring/{id}/competitors/dynamics` — снять галочки с части конкурентов → «Собрать отчёт» → в строке «Выбрано N + свой домен»; повтор с тем же набором — предупреждение о дубликате.

## 3.5.125-dev — 2026-05-29

- **Динамика конкурентов:** удаление отчёта снимает job из очереди `monitoring_change_dates`; уже запущенный расчёт на lk прерывается при следующем шаге (запись удалена из БД).
- **Проверка:** удалить «В процессе» — в `jobs` нет orphan MonitoringChangesDateQueue; CPU на lk спадает после текущего пакета ключей.

## 3.5.124-dev — 2026-05-29

- **Динамика конкурентов:** отчёты по проекту **строго по одному** — новые в статусе `pending` («Ожидает N из M»), job на lk только для первого; после готовности/ошибки автоматически стартует следующий.
- **Зависшие** `in process` >20 мин → `fail` и освобождение очереди.
- **Проверка:** натыкать 3 разных периода — один «В процессе N%», остальные «Ожидает»; без F5 процент растёт, по готовности следующий стартует.

## 3.5.123-dev — 2026-05-29

- **Динамика конкурентов:** live-обновление «Готовые отчёты» — один batch-poll раз в 3 с (вместо N параллельных POST), снятие блокировки сессии Laravel; при возврате на вкладку — мгновенный опрос.
- **Проверка:** несколько «В процессе» без F5 — процент и «Показать» появляются сами.

## 3.5.122-dev — 2026-05-29

- **Динамика конкурентов:** подсказка для большого периода — расчёт на сервере (очередь), вкладку можно закрыть; убрано «не закрывайте вкладку».
- **Проверка:** выбрать год → жёлтый hint и confirm без требования держать вкладку открытой.

## 3.5.121-dev — 2026-05-29

- **Динамика конкурентов:** удаление строки в «Готовые отчёты» — модальное подтверждение с периодом, без `confirm()`.
- **Проверка:** корзина в таблице → «Удалить отчёт?» → «Удалить».

## 3.5.120-dev — 2026-05-29

- **Динамика конкурентов:** прогресс отчёта по пакетам ключей (не только по снимкам) — процент растёт 20→40→…; полоска progress bar в колонке «Действия».
- **Проверка:** «Собрать отчёт» на проекте с большим ядром — «В процессе N%» и `1 / 5` … `5 / 5`, не залипает на 0%.

## 3.5.119-dev — 2026-05-29

- **Динамика конкурентов:** выровнены форма (кнопка «Собрать отчёт» по нижнему краю полей) и колонка «Действия» — прогресс и корзина в одну строку, без дубля «В процессе».
- **Проверка:** `/monitoring/509/competitors/dynamics` — toolbar и строки таблицы в одной высоте.

## 3.5.118-dev — 2026-05-29

- **Сравнение позиций:** удаление конкурента из заголовка столбца — через модальное окно подтверждения (как на списке конкурентов), без `confirm()`.
- **Проверка:** `/monitoring/509/competitors/positions` → корзина у домена → «Удалить конкурента?» → «Удалить» — столбец скрывается.

## 3.5.117-dev — 2026-05-29

- **Динамика конкурентов:** строка в «Готовые отчёты» появляется сразу («Запускаем…»), кнопка блокируется; повторный клик за тот же период/регион не создаёт дубликат (клиент + сервер).
- **Проверка:** «Собрать отчёт» — строка мгновенно, второй клик → предупреждение, не 4 задачи.

## 3.5.116-dev — 2026-05-29

- **Сравнение позиций:** при наведении на заголовок домена — мгновенная подсказка с полным именем (CSS, не системный `title`) и подсветка всего столбца.
- **Проверка:** `/monitoring/509/competitors/positions` → навести на `MEDTE…` — сразу `medtehno.ru`, колонка подсвечивается.

## 3.5.115-dev — 2026-05-29

- **Динамика конкурентов** (`/competitors/dynamics`): выровнена форма (регион · период · кнопка в одну строку), компактная шапка, таблица отчётов с фиксированными колонками и ellipsis в регионе.
- **Проверка:** `/monitoring/509/competitors/dynamics` — кнопка не на всю ширину, «Показать» рядом с периодом.

## 3.5.114-dev — 2026-05-29

- **Конкуренты:** вкладка **«Динамика конкурентов»** в sub-nav (`/monitoring/{id}/competitors/dynamics`) — весь блок «по датам» перенесён с `/competitors/positions`. На «Сравнение с конкурентами» осталась только таблица позиций.
- **JS:** `cabinet-monitoring-competitors-dynamics.js` — сбор отчётов, опрос очереди, календарь.
- **Проверка:** sub-nav — три вкладки; динамика открывается сразу без загрузки таблицы позиций.

## 3.5.113-dev — 2026-05-29

- **Отчёт по датам** (`/monitoring/competitors/result-analyse/{id}`): клик по домену в легенде графика скрывает/показывает линию; отключённые — серым и перечёркнутым (как легенда на `/monitoring/{id}`).
- **Проверка:** отчёт id 63 — клик по `apteka.ru` в легенде, линия исчезает, подпись серая с зачёркиванием; повторный клик возвращает.

## 3.5.112-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** таблица позиций не грузится при открытии вкладки — только по кнопке «Загрузить сравнение позиций»; блок «Динамика по датам» доступен сразу. Регион вынесен в общую панель сверху.
- **Проверка:** `/monitoring/{id}/competitors/positions` — без спиннера позиций, «Показать» у готового отчёта сразу; кнопка загрузки строит таблицу.

## 3.5.111-dev — 2026-05-29

- **Динамика по датам:** прогресс сбора **N%** и «снимков X / Y»; предупреждение и confirm для больших периодов; статус «Похоже, зависло» если progress не обновлялся >20 мин.
- **Backend:** progress в `request` JSON при bulk-расчёте; `POST /monitoring/competitors/history/estimate`.
- **Проверка:** период 07/2025–05/2026 (~24 снимка) — confirm ~10 мин, в строке «В процессе 42% · 10 / 24 снимков».

## 3.5.110-dev — 2026-05-29

- **Отчёт по датам:** таблица — свежие снимки сверху; дельта считается к предыдущему снимку по времени.
- **Проверка:** `/monitoring/competitors/result-analyse/63` — 26.05.2026 в первой строке.

## 3.5.109-dev — 2026-05-29

- **Конкуренты / отчёт по датам** (`/monitoring/competitors/result-analyse/{id}`): новый UI — линейные графики по метрикам (средняя, ТОП‑3/10/100), таблица «дата × домен», лидер дня, дельта к прошлому снимку, переключатель график/таблица.
- **Проверка:** открыть готовый отчёт (например id 63) — Chart.js + компактная таблица вместо широкой матрицы 4×N.

## 3.5.108-dev — 2026-05-29

- **Конкуренты / динамика по датам:** ускорен расчёт отчёта — только **дни со снимком** мониторинга (не каждый календарный день), bulk SQL как на `/competitors/positions` вместо ~165k jobs `monitoring_helper` на большом ядре.
- **Config:** `competitors_changes_dates_use_bulk` (default `true`; `false` — старый fan-out).
- **Проверка:** период 01-07-2025–31-05-2026, «Собрать отчёт» → `/monitoring/competitors/result-analyse/{id}` заметно быстрее.

## 3.5.107-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** таблица «Готовые отчёты» — свежие сверху (сортировка по id desc, новый отчёт вставляется в начало).
- **Проверка:** запустить «Собрать отчёт» — строка появляется первой в списке.

## 3.5.106-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** календарь периода — все пресеты как в TOP‑100 (7/30/60/90/180/365 дней, прошлый месяц) через `cabinet-monitoring-date-range.js`.
- **Проверка:** блок «Динамика конкурентов» — справа в календаре 7 пунктов быстрого выбора.

## 3.5.105-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** блок «Динамика конкурентов по датам» — понятнее назначение (смена лидерства, день к дню), кнопка «Собрать отчёт», список готовых отчётов.
- **Проверка:** `/monitoring/509/competitors/positions` — тексты блока внизу страницы.

## 3.5.104-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** исправлена пустая таблица (все «0») — лимит SQL снова `queries × 100` SERP-строк на ключ (ошибочный `× domains × 3` отрезал данные); сопоставление доменов без учёта регистра; в ячейках «нет в топе» → **101**.
- **Проверка:** `/monitoring/509/competitors/positions` — позиции 1–100, не нули.

## 3.5.103-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** колонки доменов фиксированной ширины (~4.75rem), длинные названия обрезаются с ellipsis и `title` при наведении.
- **Проверка:** `/monitoring/509/competitors/positions` — `BRAVOKISLOROD.RU` не шире соседних колонок.

## 3.5.102-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** блок **ТОП-50** в сводке; bulk быстрее — чанки без промежуточной статистики, limit SQL ≈ `queries×domains×3` (было ×100), bulk-chunk 1000, графики отложены на 60 ms после таблицы.
- **Проверка:** `/monitoring/509/competitors/positions` — ТОП-50 между 30 и 100, загрузка быстрее.

## 3.5.101-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** в сводке добавлен блок **ТОП-30** (график + таблица) между ТОП-10 и ТОП-100.
- **Проверка:** `/monitoring/509/competitors/positions` → после загрузки раскрыть «Процент попадания в топ 30».

## 3.5.100-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** bulk больше не шлёт 4925 ключей в POST (лимит `max_input_vars`) — ключи читаются на сервере; убран ложный toast при fallback; sub-nav выровнен с chrome; колонка своего сайта — голубая шапка + badge «Ваш сайт»; кнопки в истории анализов на одной линии.
- **Проверка:** `/monitoring/509/competitors/positions` — без красного toast, bulk с первого раза, almamed.su выделена в таблице.

## 3.5.99-dev — 2026-05-29

- **Конкуренты / сравнение позиций — скорость v2:** один bulk POST (`bulk=1`) — все ~4925 запросов обрабатываются на сервере батчами по 500 без 20 HTTP round-trip; fallback на параллельные AJAX при ошибке.
- **UX загрузки:** блок прогресса по центру карточки; таблица скрыта до готовности (нет «Пусто» на 20%).
- **Проверка:** `/monitoring/509/competitors/positions` — спиннер по центру, затем таблица целиком.

## 3.5.98-dev — 2026-05-29

- **Конкуренты / сравнение позиций — скорость:** параллельная загрузка батчей (5 одновременно, по 250 запросов вместо 100); снимок даты выдачи один раз на регион (без повторного `getLastDateByWords` в каждом AJAX); DataTable не перерисовывается после каждого батча.
- **Проверка:** `/monitoring/509/competitors/positions` — прогресс до 100% заметно быстрее (~20 батчей вместо ~50 последовательных).

## 3.5.97-dev — 2026-05-29

- **Конкуренты / сравнение позиций:** отступы у таблицы запрос×домены (padding ячеек и контейнера, как у подбора конкурентов).
- **Проверка:** `/monitoring/509/competitors/positions` — колонка «Запрос» и крайние домены не прилипают к рамке карточки.

## 3.5.96-dev — 2026-05-29

- **Конкуренты / сравнение позиций** (`/monitoring/{id}/competitors/positions`): убраны small-box и inline-стили; workspace + sub-nav, dt-bar, прогресс загрузки, блоки графиков, история по датам — как у остальных подмодулей мониторинга. JS → `cabinet-monitoring-competitors-positions.js`.
- **Проверка:** `/monitoring/509/competitors/positions` — chrome, вкладки «Подбор» / «Сравнение», таблица запрос×домены, сводка ТОП, блок «Изменения по ТОП и дате».

## 3.5.95-dev — 2026-05-29

- **Конкуренты:** страница приведена к workspace-эталону (шапка с метой, toolbar «Мои конкуренты», dt-bar, empty/ready/loading/table); modals в partial; CSS/JS синхронизированы с новыми классами.
- **Проверка:** `/monitoring/509/competitors` — шапка «Конкуренты из топ-10», chips, подсказки, анализ → таблица, overlay при AJAX метрик.

## 3.5.94-dev — 2026-05-29

- **Конкуренты:** при догрузке ТОП/средней — спиннер в ячейках и полоска «Считаем ТОП…» над таблицей (тот же `cabinet-mon-loader`, что при анализе).
- **Проверка:** `/monitoring/509/competitors` → анализ → колонки ТОП-3/10/100 и «Средняя» с кружком до появления цифр.

## 3.5.93-dev — 2026-05-29

- **Конкуренты / производительность:** основной анализ снова только по топ-10 (быстро, ~2050 доменов); ТОП-%, средняя и пересечение по топ-100 — отдельным AJAX для видимой страницы таблицы (50 строк).
- **Проверка:** `/monitoring/509/competitors` → «Анализировать» — таблица появляется быстро; метрики ТОП/средняя догружаются через «…» на текущей странице.

## 3.5.92-dev — 2026-05-29

- **Конкуренты:** строка пагинации DataTables на русском («Показано с 1 по 50 из 2 050 записей»).
- **Проверка:** `/monitoring/509/competitors` → анализ → подвал таблицы.

## 3.5.91-dev — 2026-05-29

- **Конкуренты / подбор из топ-10:** предлагается до 10 доменов (было 5); `yandex.ru` в игнорируемых (конфиг + блок «Игнорируемые домены»).
- **Проверка:** `/monitoring/509/competitors` → анализ → «Подобрать из топ-10» — без yandex.ru, до 10 строк.

## 3.5.90-dev — 2026-05-29

- **Конкуренты / таблица:** колонки пересечения с ядром (число + %), ТОП-3/10/100, средняя позиция; метрики считаются при анализе по всем запросам проекта.
- **Проверка:** `/monitoring/509/competitors` → «Анализировать» — в таблице новые колонки между ПС и видимостью.

## 3.5.89-dev — 2026-05-29

- **Конкуренты:** «Подобрать из топ-10» теперь кликабельна всегда — если таблица ещё не загружена, кнопка запускает анализ и автоматически открывает подбор после загрузки.
- **Проверка:** `/monitoring/509/competitors` → нажать «Подобрать из топ-10» на экране «готово к сравнению».

## 3.5.88-dev — 2026-05-29

- **Конкуренты / таблица:** поиск слева, пагинация справа; компактные чипы видимости; подсветка своего сайта и выбранных конкурентов; аккуратнее домен и иконки ПС.
- **Проверка:** `/monitoring/509/competitors` → «Анализировать».

## 3.5.87-dev — 2026-05-29

- **Цена запросов:** в панели таблицы поиск слева, выбор количества строк справа.
- **Проверка:** `/monitoring/509/prices`.

## 3.5.86-dev — 2026-05-29

- **Конкуренты:** если конкуренты уже есть — вместо «Шаг 1» показывается экран «Сравнить позиции»; липкая панель внизу только сразу после добавления в таблице.
- **Проверка:** `/monitoring/509/competitors` с 9 конкурентами — по центру зелёный CTA, не пустой шаг 1.

## 3.5.85-dev — 2026-05-29

- **Конкуренты:** подсказки со стрелками (coach) на 3 шага; после добавления конкурента — зелёная липкая панель «Сравнить позиции» внизу экрана.
- **Проверка:** `/monitoring/509/competitors` — анализ → галочка → всплывающая подсказка и панель внизу.

## 3.5.84-dev — 2026-05-29

- **Конкуренты:** пошаговый сценарий (1 анализ → 2 отметка → 3 сравнение), кнопка «Добавить вручную» без анализа, подсказка над таблицей, галочка без лишнего confirm, чипы обновляются сразу.
- **Проверка:** `/monitoring/509/competitors` — «Добавить вручную» → deal-med.ru; или анализ → галочка в таблице.

## 3.5.83-dev — 2026-05-29

- **Конкуренты:** пересборка workspace — intro в карточке, сетка регион+анализ, панель конкурентов (заголовок/кнопки → чипы), один CTA, dt-bar как на ценах, компактный empty state.
- **Проверка:** `/monitoring/509/competitors` — без дубля «Анализировать», выровненные блоки.

## 3.5.82-dev — 2026-05-29

- **ТОП-100:** календарь как на `/monitoring/{id}` — общий `cabinet-monitoring-date-range.js`: пресеты 7/30/60/90/180/365/прошлый месяц, режимы дат, подсветка снимков.
- **Проверка:** `/monitoring/top-100/509` — открыть «Диапазон дат», сравнить с таблицей позиций.

## 3.5.81-dev — 2026-05-29

- **Цены:** компактная полоса DataTables (число строк слева, поиск справа без «растягивания»), шапка регион+бюджет без лишнего зазора.
- **Группы:** поиск в той же типографике, что цены.
- **Проверка:** `/monitoring/509/prices` — полоса над таблицей плотная, инпуты с рамкой.

## 3.5.80-dev — 2026-05-29

- **Группы** (`/monitoring/{id}/groups`): project-chrome, workspace-карточка, вынесенный `cabinet-monitoring-groups.js`, компактная таблица с действиями в строке, раскрытие сводки ТОП, счётчики групп/запросов.
- **Проверка:** `/monitoring/509/groups` — навигация как у конкурентов/цен, поиск справа, кнопки CRUD в тулбаре.

## 3.5.79-dev — 2026-05-29

- **ТОП-100:** одна карточка-workspace, компактная сетка полей (как цены), empty state вместо пустой панели, select2 и инпуты одной высоты.
- **Проверка:** `/monitoring/top-100/730` — форма в 2 плотных строки, CTA в центре до анализа.

## 3.5.78-dev — 2026-05-29

- **Цены запросов:** одна карточка-workspace; подписи регион/бюджет; строка «показать N / поиск» над таблицей; компактная типографика TOP-колонок, иконка редактирования по hover.
- **Проверка:** `/monitoring/509/prices` — без разрыва между регионом и бюджетом, ровная шапка таблицы.

## 3.5.77-dev — 2026-05-29

- **Конкуренты:** одна рабочая карточка вместо серой дыры — шапка с регионом и «Анализ», полоска чипов конкурентов, empty state с CTA, лоадер и таблица внутри белой панели.
- **Проверка:** `/monitoring/509/competitors` — до анализа виден empty state, не пустой серый фон; после «Анализ» — таблица.

## 3.5.76-dev — 2026-05-29

- **Цены запросов** (`/monitoring/{id}/prices`): chrome модуля позиций; тулбар региона + бюджет; inline-редактирование ячеек (Enter/Esc); массовое редактирование через модалку; без DataTables Editor — `cabinet-monitoring-prices.js`.
- **Проверка:** http://localhost:3002/monitoring/509/prices — регион, клик по TOP 10, выбор строк → «Изменить выбранные», сохранение бюджета.

## 3.5.75-dev — 2026-05-29

- **ТОП-100** (`/monitoring/top-100/{id}`): chrome как у `/monitoring/{id}`; панель настроек LTE4, kanban-колонки в workspace, прогресс-лоадер; JS в `cabinet-monitoring-top100.js`.
- **Проверка:** http://localhost:3002/monitoring/top-100/730 — фраза + даты + «Анализ» → колонки по датам, подсветка домена/конкурентов, связи позиций.

## 3.5.74-dev — 2026-05-29

- **Конкуренты проекта** (`/monitoring/{id}/competitors`): chrome как у `/monitoring/{id}` — breadcrumb, favicon, module-nav; KPI-счётчик, тулбар региона, таблица в `cabinet-mon-project-table-panel`; JS в `cabinet-monitoring-competitors.js`.
- **Проверка:** http://localhost:3002/monitoring/646/competitors — визуально как модуль позиций; «Анализ» → таблица доменов; список конкурентов, модалки LTE4.

## 3.5.73-dev — 2026-05-29

- **KPI «Ключевых слов»:** выравнивание цифры с соседними плитками (подпись «в проекте» + единая высота шапки карточки).
- **Проверка:** `/monitoring/646?region=…` — 413 на одной линии с «Средняя позиция».

## 3.5.72-dev — 2026-05-29

- **Скорость таблицы:** убран дублирующий POST `/monitoring/{id}/table` — первый ответ переиспользуется в DataTables (раньше один и тот же тяжёлый запрос шёл дважды).
- **Проверка:** Network на `/monitoring/646?region=…#keywords` — один POST `/table` при первой загрузке; время до таблицы ~в 2 раза меньше.

## 3.5.71-dev — 2026-05-29

- **Загрузка таблицы (финал):** один путь — спиннер до relayout scrollX под ним; убраны `visibility:hidden`, `is-relayouting` (opacity:0) и раннее снятие лоадера без relayout.
- **Проверка:** hard refresh `/monitoring/646?region=…#keywords` — только «Загрузка…» → таблица, без пустой белой фазы.

## 3.5.70-dev — 2026-05-29

- **Загрузка таблицы:** relayout scrollX под спиннером, без `is-relayouting` (opacity:0) после снятия лоадера — убрана пустая серая фаза.
- **Проверка:** hard refresh `/monitoring/646?region=…#keywords` — спиннер до готовой таблицы, без промежуточной пустоты.

## 3.5.69-dev — 2026-05-29

- **Тулбар «Столбцы»:** убран дубль в подвале — `<template>` Blade не отдавал в HTML, `@include` controls рендерился видимым блоком после модалок; источник перенесён в скрытый `#cabinetMonTableControlsTpl`.
- **Проверка:** hard refresh `/monitoring/646?region=…#keywords` — тулбар только над таблицей, внизу страницы пусто.

## 3.5.68-dev — 2026-05-29

- **Загрузка таблицы:** спиннер до появления строк в DOM (не по раннему `drawCallback`); серый `processing` DataTables скрыт под лоадером — без пустой области.
- **Проверка:** hard refresh `/monitoring/646?region=…` — «Загрузка таблицы…» до полной таблицы, без промежуточной пустоты.

## 3.5.67-dev — 2026-05-29

- **Колонка «Запрос»:** шире (380px, min 22rem) — меньше переносов длинных фраз.
- **Проверка:** `/monitoring/{id}` → Ключевые слова, режим одного региона с датами.

## 3.5.66-dev — 2026-05-29

- **Таблица ключей:** убран дублирующий тулбар «Столбцы» под таблицей (второй `mailbox-controls` в dom DataTables).
- **Проверка:** `/monitoring/{id}` → Ключевые слова — переключатели столбцов только над таблицей.

## 3.5.65-dev — 2026-05-29

- **Загрузка таблицы:** спиннер до полной сборки (данные + тулбар), без пустой серой области между лоадером и таблицей.
- **Проверка:** hard refresh `/monitoring/{id}` → «Загрузка таблицы…» крутится до появления строк и фильтров.

## 3.5.64-dev — 2026-05-29

- **Удаление запроса:** модалка подтверждения LTE4 вместо `window.confirm` — из строки, из формы редактирования и массово из тулбара.
- **Проверка:** `/monitoring/{id}` → корзина в строке → «Удалить запрос?» с кнопками Отмена / Удалить.

## 3.5.63-dev — 2026-05-29

- **Модалки запросов (LTE4):** `btn-close`, `form-label`, `btn-primary` / `outline-secondary`; русские подписи; в редактировании — «Удалить» с `bi-trash`; корзина в тулбаре таблицы — Bootstrap Icons.
- **Проверка:** `/monitoring/{id}` → карандаш в строке — модалка на русском, кнопки LTE4; удаление из модалки и из строки.

## 3.5.62-dev — 2026-05-29

- **Иконка целевого URL у запроса:** приглушённая серая, без синего бейджа; заметнее при наведении на строку.
- **Проверка:** `/monitoring/{id}` → Ключевые слова — иконка ссылки не бросается в глаза.

## 3.5.61-dev — 2026-05-29

- **Перезагрузка страницы проекта:** без моргания «всех столбцов» — видимость колонок с сервера в DataTables до первой отрисовки; тулбар из `<template>`, не отдельный AJAX; фильтр группы в первом запросе (без `setTimeout` 1 с); вкладка Обзор/Ключевые слова через `data-view` до paint.
- **Проверка:** `/monitoring/646?group=…` hard refresh — сразу нужные колонки и строки, без вспышки лишних регионов/столбцов.

## 3.5.60-dev — 2026-05-29

- **Иконка целевого URL у запроса:** заметный синий бейдж вместо бледного `badge-light`.
- **Проверка:** `/monitoring/{id}` → Ключевые слова → у запроса с целевым URL иконка ссылки хорошо видна, popover по клику.

## 3.5.59-dev — 2026-05-29

- **Графики «Все регионы»:** вкладка «% в ТОП» первая и открыта по умолчанию; при ≥4 регионах — сразу график TOP-10, пресеты TOP-1/3 скрыты.
- **Проверка:** `/monitoring/646` без `?region=` → Обзор → сразу % в ТОП по регионам, пресет TOP-10.

## 3.5.58-dev — 2026-05-29

- **Графики «Все регионы»:** убрана текстовая подсказка над вкладками (про пресеты и инвертированную ось).
- **Проверка:** `/monitoring/{id}` без `?region=` → Обзор — только заголовок и вкладки, без абзаца под ними.

## 3.5.57-dev — 2026-05-29

- **Действия в строке таблицы (карандаш/корзина):** модалка не открывалась — `data-target=".modal"` не мигрировался в BS5; явный `#cabinetMonKeywordsModal`, программный вызов для строк DataTables; кнопки приглушены, ярче при наведении на строку.
- **Проверка:** `/monitoring/{id}` → вкладка «Ключевые слова» → карандаш открывает форму редактирования; корзина — confirm и обновление строки.

## 3.5.56-dev — 2026-05-29

- **График «% в ТОП по регионам»:** в режиме «Все регионы» вторая вкладка — линия на регион (TOP-1/3/10), API `chart=regions_top`, ось 0–100%; фильтр «По дням/неделям/месяцам» на обеих вкладках.
- **Проверка:** проект с ≥2 регионами без `?region=` → Обзор → «% в ТОП» → пресеты TOP-10 и др.; линии по городам, значения ≤100%.

## 3.5.55-dev — 2026-05-29

- **Таблица при «Обзор» ↔ «Ключевые слова»:** без `display:none` (DataTables scrollX сохраняет ширины); один `columns.adjust()` без повторных скачков; краткий `is-relayouting` при возврате.
- **Проверка:** `/monitoring/57?region=…` — переключить вкладки несколько раз: шапка и колонки не «дрыгаются».

## 3.5.54-dev — 2026-05-29

- **KPI «Все регионы»:** частичный снимок (только `top1`, остальное `null`) снова уходит в live-расчёт — все карточки ТОП заполнены.
- **Подпись города:** под «В ТОП-1/3/…» и «Средняя позиция» — город региона или «все регионы» меньшим шрифтом.
- **График по регионам:** при &lt;2 точек за период подтягивается история за 90 дней — линии не «пустые».
- **Проверка:** `/monitoring/57` без `?region=` — KPI полные, подпись «все регионы»; `?region=…` — город; Обзор → средняя по регионам с линиями.

## 3.5.53-dev — 2026-05-29

- **График «% в ТОП»:** дубли позиций при одинаковом `created_at` давали >100% (до 163%) и ломали шкалу; последняя позиция в корзине — по `MAX(id)`, дедуп по `monitoring_keyword_id`; ось Y зафиксирована 0–100%; «По дням» снова даёт дневную детализацию.
- **Проверка:** `/monitoring/57?region=100` → Обзор → % в ТОП, TOP-10 — значения ≤100%, линия читаема; при «По дням» больше точек, чем 3 месяца.

## 3.5.52-dev — 2026-05-29

- **KPI — скорость:** сводка рендерится в HTML при открытии страницы (без отдельного AJAX ~10 с); снимок читается напрямую из БД, live-расчёт — одним SQL вместо N подзапросов; `project-stats?summaryOnly=1` без `exportGroupsForProject`.
- **Проверка:** `/monitoring/57` — карточки KPI сразу с цифрами, без долгого спиннера; Network без тяжёлого `project-stats` на первой загрузке.

## 3.5.51-dev — 2026-05-29

- **Кольцевая «Распределение по ТОП-100» — легенда:** клик по кружку переключает один сегмент (`toggleDataVisibility`), а не весь график; скрытые пункты — серый маркер, бледный текст, зачёркивание (`strikethrough`).
- **Проверка:** `/monitoring/57` → Обзор → Распределение по ТОП-100 — клик по 1–2 сегментам: остальные видны, подписи скрытых зачёркнуты.

## 3.5.50-dev — 2026-05-29

- **KPI «Все поисковые системы и регионы»:** если снимок списка проектов без ТОП-% (устаревший кэш, `top1` пустой при наличии слов) — live-расчёт по всем регионам, карточки не остаются «—».
- **Проверка:** `/monitoring/57` без `?region=` — В ТОП-1/3/10, средняя позиция, число ключей заполнены; hard refresh.

## 3.5.49-dev — 2026-05-29

- **Графики — легенда:** при клике скрытый сегмент/серия — серый маркер, бледный текст, зачёркивание (кольцевая «Распределение» читала `ds.hidden` вместо `meta.data[i].hidden`).
- **Проверка:** `/monitoring/83?region=127` → Обзор → «Распределение по ТОП-100» / «Средняя позиция» — клик по кружку в легенде, визуал «выключено».

## 3.5.48-dev — 2026-05-29

- **Попап URL в выдаче:** нерелевантные ссылки снова `text-danger`, целевые — `text-success`; длинные URL переносятся, попап в `body` с `preventOverflow` — не вылезает за экран.
- **Бейдж счётчика:** красный/зелёный на числе не перебивается стилем `.cabinet-mon-url-badge`.
- **Проверка:** `/monitoring/77?region=118` → навести на иконку ссылки — красные несовпадающие URL, попап в пределах окна.

## 3.5.47-dev — 2026-05-29

- **Таблица после «Обзор»:** при возврате на «Ключевые слова» (в т.ч. после вкладки «Распределение по ТОП-100») вызывается `columns.adjust()` — шапка и строки DataTables `scrollX` снова совпадают.
- **Проверка:** `/monitoring/83?region=127` → Обзор → Распределение по ТОП-100 → Ключевые слова — колонки не «плывут».

## 3.5.46-dev — 2026-05-29

- **«Показать нецелевые URL»:** фильтр снова работает — `whereIn` по `monitoring_keywords.id` (без ambiguous `id` после `joinGroup`); пустой результат — явный `0 = 1`.
- **Вёрстка:** переключатель и подпись в одной строке с «Динамика» (`align-items: center`, центрирование трека тумблера).
- **Проверка:** `/monitoring/77?region=118` → включить тумблер — ~251 строка вместо 538; подпись на одной линии с селектом.

## 3.5.45-dev — 2026-05-29

- **Поиск по «Запрос»:** server-side фильтр понимает альтернативы раскладки из `cabinet-monitoring-search.js` (`pote|зщеу` → `LIKE %pote%` OR `LIKE %зщеу%`), а не буквальную строку с `|`. То же для глобального поиска таблицы.
- **Проверка:** `/monitoring/83?region=127` → лупа в колонке «Запрос» → `pote`, `potec`, русский текст, ввод в другой раскладке — строки находятся.

## 3.5.44-dev — 2026-05-29

- **Вёрстка:** убрана горизонтальная прокрутка страницы (шапка — `flex-wrap`, `overflow-x: clip`; таблица — скролл только внутри `#monitoringTable_wrapper`, без `sticky` на `scrollHead`). Иконка выхода не вылезает за край.
- **Проверка:** `/monitoring/83?region=127` — нет полосы прокрутки у всего окна; у таблицы — только внутренняя.

## 3.5.43-dev — 2026-05-29

- **Попап URL в выдаче:** контент снова в `data-bs-content` (Bootstrap 5); инициализация через `bootstrap.Popover` вместо jQuery `.popover()`.
- **Проверка:** `/monitoring/83?region=127` → навести на бейдж с числом в колонке «URL в выдаче» — список URL с датами.

## 3.5.42-dev — 2026-05-29

- **Таблица — «Столбцы»:** чипы в одной строке с кнопками действий (не отдельная панель ниже); стиль `btn-outline-secondary` / LTE4 — серый фон, без насыщенного синего.
- **Проверка:** `/monitoring/83?region=127` → вкладка «Ключевые слова» → тулбар над таблицей.

## 3.5.41-dev — 2026-05-29

- **Кольцевая «Распределение по ТОП-100»:** палитра Bootstrap 5 / AdminLTE 4 (`html/index2.html`, donut): primary → teal → info → warning → secondary → danger; белые разделители сегментов, `cutout 62%`, легенда компактнее (13px).
- **Проверка:** `/monitoring/83?region=127` → вкладка «Распределение по ТОП-100» (при сравнении — оба кольца в одной палитре).

## 3.5.40-dev — 2026-05-29

- **Select2 «Сравнить с проектом»:** при первом клике фокус сразу в поле поиска (`cabinet-select2-defaults.js`: `mousedown` + `focus` на `.select2-search__field`). На `/monitoring/{id}` скрипт подключается **повторно после** `select2.full.min.js` в `show.blade.php` (в layout он грузится раньше плагина и обработчики не вешались).
- **Проверка:** `/monitoring/83?region=127` → клик по сравнению → сразу печатать название проекта.

## 3.5.39-dev — 2026-05-29

- **«Показать нецелевые URL»:** исправлена логика (фильтр по `checked`, не по `.val()`); бэкенд — `pluck(monitoring_keyword_id)` вместо id позиции. Переключатель с цветом OFF/ON (красный/зелёный) и зелёной подписью в активном состоянии; toastr, если нецелевых строк нет; tooltip про «Целевой URL».
- **Проверка:** `/monitoring/83?region=127` (один регион) → вкладка ключей → включить тумблер — в таблице только строки, где URL в выдаче ≠ целевой странице.

## 3.5.38-dev — 2026-05-29

- **Колонка «Цель в топе»:** вместо яркого `badge-info` — спокойный серо-синий чип `cabinet-mon-target-badge` (не перетягивает внимание с позиций).
- **Проверка:** `/monitoring/83` → вкладка ключей — числа цели в топе читаются, но не «кричат».

## 3.5.37-dev — 2026-05-29

- **Таблица — чипы «Столбцы»:** активное состояние без насыщенного синего — светло-серый фон, спокойная рамка и галочка в `--mon-proj-accent` (не перетягивает внимание с таблицы).
- **Проверка:** `/monitoring/83` → вкладка ключей — включённые столбцы читаются, но не «кричат».

## 3.5.36-dev — 2026-05-29

- **Лоадеры `/monitoring/{id}`:** вместо статичного кольца — **вращающаяся** иконка `fa-circle-notch` + пульсация подписи. Перебивает глобальное `prefers-reduced-motion` из AdminLTE 4 (иначе анимация ≈0 ms и кажется зависшей). KPI, таблица, графики.
- **Проверка:** `/monitoring/83` — при загрузке KPI, таблицы и графика спиннер явно крутится (hard refresh если кэш CSS).

## 3.5.35-dev — 2026-05-29

- **Таблица запросов — столбцы:** отдельная панель «Столбцы» (не в одной линии с кнопками); чипы с галочкой (виден) / пунктир (скрыт), белый фон панели на сером тулбаре; подсказка «синий — виден, пунктир — скрыт». Иконка URL в выдаче — заметный синий бейдж. Дублирующий тулбар снизу таблицы скрыт.
- **Проверка:** `/monitoring/83` → вкладка ключей — панель столбцов читается без наведения.

## 3.5.34-dev — 2026-05-29

- **Распределение по ТОП-100 при сравнении:** учитываются **общие запросы** (как на графике % в ТОП); при выбранном конкуренте — **два** пончика рядом (основной проект и сравниваемый), перезагрузка при смене сравнения.
- **Проверка:** `/monitoring/83?region=127&group=1152` → oftalmag.ru → вкладка «Распределение по ТОП-100».

## 3.5.33-dev — 2026-05-29

- **Таблица запросов:** переключатели столбцов — подпись **«Столбцы:»**, включённый столбец синий (`is-on`), скрытый серый (`is-off`); tooltip «Скрыть или показать столбец».
- **Проверка:** `/monitoring/83` → вкладка с таблицей ключей — видно, какие столбцы активны.

## 3.5.32-dev — 2026-05-29

- **Сравнение проектов — честная база:** при двух выбранных папках графики считают % только по **общим запросам** (пересечение текстов ключей), а не по всем ключам каждой папки. Плашка: «Сравнение по N общим запросам…». Пример: oftal-med «Авторефрактометры» (417) vs oftalmag «Авторефрактометры» (23) → база **23**, не 417 и не 23 отдельно.
- **Проверка:** `/monitoring/83?region=127&group=1152` → сравнить с oftalmag.ru, та же папка → плашка «23 общих запроса», TOP-10 у основного ~39%, у конкурента ~61% (одинаковый знаменатель).

## 3.5.31-dev — 2026-05-29

- **Сравнение проектов (Select2):** при открытии списка фокус сразу в поле поиска — можно печатать без второго клика.
- **Проверка:** `/monitoring/83` → клик «Сравнить с проектом» → сразу вводите название.

## 3.5.30-dev — 2026-05-29

- **Графики за длинный период:** «Не удалось загрузить график» на год+ (например kawe.su #77) — не грузим сотни тысяч позиций в память; агрегация в SQL (`MonitoringChartPositionSeries`). Для периода **> 45 дней** — точки по неделям, **> 120 дней** — по месяцам (авто, если не выбран фильтр «недели/месяцы»).
- **Проверка:** `/monitoring/77?dates=2025-05-30+-+2026-05-29` → «Все регионы» → график «Средняя позиция по регионам» строится без ошибки.

## 3.5.29-dev — 2026-05-29

- **График «Средняя позиция по регионам»:** в легенде у каждой линии указана ПС — **Яндекс** / **Google** (как в фильтре «Поисковая система»), чтобы не путать, например, «Москва [213]» и «Москва [1011969]».
- **Проверка:** `/monitoring/83` → «Все поисковые системы и регионы» — легенда: `Яндекс Москва, … [213]`, `Google Москва [1011969]`.

## 3.5.28-dev — 2026-05-29

- **Сравнение + TOP-10:** при открытии по прямой ссылке линии больше не оба синие — первая загрузка графика % в ТОП идёт тем же путём, что и клик по кнопке TOP-10 (`applyTopChartFromRaw`).

## 3.5.27-dev — 2026-05-29

- **Сравнение проектов:** кнопка сброса (×) в Select2 — ровно по центру, без серого кружка; стрелка и текст не наезжают друг на друга.

## 3.5.26-dev — 2026-05-29

- **Цвета графиков / сравнение:** при двух проектах на TOP-10 линии больше не оба синие — `applyDistinctLineColors` после пресета; насыщенная палитра (`MonitoringChartPalette` + `cabinet-monitoring-chart-scales.js`). Правило: `.cursor/rules/redbox-cabinet-charts.mdc`.
- **Проверка:** `/monitoring/83` → Москва → сравнить с `oftalmag.ru` → TOP-10 — синий и оранжевый в легенде.

## 3.5.25-dev — 2026-05-29

- **График «Средняя позиция по регионам»:** не обнулялся, если у **части** регионов нет снимков за период — рисуем линии по регионам с данными (баг: первый пустой регион сбрасывал весь график). Календарь периода виден и в режиме «Все регионы»; для API — дефолт 30 дней, если `dates` не в URL.

## 3.5.24-dev — 2026-05-29

- **Сравнение — уведомление о регионе:** вместо оранжевого toast — **постоянная плашка** под полем «Сравнить с проектом» (не исчезает, пока не смените выбор). Список доступных регионов — тегами.

## 3.5.23-dev — 2026-05-29

- **Сравнение проектов:** блок всегда активен — сначала выбираете конкурента, не наоборот. При смене региона **не подставляется** чужой проект из localStorage (привязка к `regionKey`). Нет региона у конкурента → toast с перечнем доступных регионов; API `strictMatch` не подменяет первый попавшийся регион.
- **Проверка:** `/monitoring/77` → «Сравнить с проектом» → выбрать сайт без Москвы при фильтре Москва — предупреждение со списком регионов.

## 3.5.22-dev — 2026-05-29

- **Сравнение проектов:** блок «Сравнить с проектом» виден на **всех** проектах. Если выбрано «Все поисковые системы и регионы» — поля неактивны и показана подсказка; выберите конкретный регион (как на kawe.su: Москва / Воронеж) — Select2 с поиском включается.
- **Проверка:** http://localhost:3002/monitoring/77 — подсказка в тулбаре; `?region=…` или один регион в проекте — поиск по названию как на `/monitoring/88`.

## 3.5.21-dev — 2026-05-29

- **Умный поиск (раскладка):** если введено «щаеф» вместо `staff` (или наоборот), поиск находит совпадения по обоим вариантам. Общий хелпер `cabinet-monitoring-search.js`: v2 список проектов (таблица и плитки), классический `/monitoring`, `/monitoring/{id}` (глобальный и по колонкам), группы, цены, конкуренты, Select2 сравнения проектов.
- **Проверка:** http://localhost:3002/monitoring-v2 — ввести название проекта в «чужой» раскладке; http://localhost:3002/monitoring — то же в фильтре таблицы.

## 3.5.20-dev — 2026-05-29

- **Сравнение проектов:** выбор проекта — **Select2 с поиском** (ввод названия), не длинный `<select>`.

## 3.5.19-dev — 2026-05-29

- **Календарь `/monitoring/{id}`:** кнопки **«Применить»** и **«Отмена»** (вместо Apply / Cancel).

## 3.5.18-dev — 2026-05-29

- **Лоадеры `/monitoring/{id}`:** свой анимированный спиннер (`cabinet-mon-loader`) — синее вращение + пульсация подписи; фазы «Загрузка графика…» / «Отрисовка графика…». KPI, таблица и графики.

## 3.5.17-dev — 2026-05-29

- **Сравнение проектов на `/monitoring/{id}`:** в тулбаре — проект для сравнения и **отдельная группа**; серии на графиках % в ТОП и средней позиции с подписью `· имя проекта`. Выбор сохраняется в `localStorage` (`cabinet-mon-show-compare-v1`) per базовый проект. Регион сопоставляется по ПС + lr (`matchEngine` / `matchLr` в `/monitoring/charts`). Распределение — только основной проект.

## 3.5.16-dev — 2026-05-29

- **Вкладка «Распределение по ТОП-100»:** селектор «По дням / неделям / месяцам» скрыт — распределение считается по **последней дате** в выбранном периоде, без временной группировки.

## 3.5.15-dev — 2026-05-29

- **Серии ТОП на `/monitoring/{id}`:** отдельные кнопки **TOP-1** и **TOP-3**; по умолчанию **TOP-10** (localStorage `cabinet-mon-show-top-preset`). API `% в ТОП` отдаёт серию ТОП-1.
- **Загрузка графиков:** счётчик параллельных запросов (спиннер до конца всех), `chart.update('none')` + `requestAnimationFrame` — спиннер не «замирает» на больших периодах.

## 3.5.14-dev — 2026-05-29

- **Загрузка на `/monitoring/{id}`:** полоска KPI показывает оверлей «Загрузка сводки…» (пока `projectStats`); у графика — подпись «Загрузка графика…» рядом со спиннером.

## 3.5.13-dev — 2026-05-29

- **Период «год»:** везде **365** дней (не 366) — календарь, настройки графиков v2, тренд портфеля; старый `366` в localStorage → `365`.

## 3.5.12-dev — 2026-05-29

- **KPI vs график:** при выбранном регионе сводка считается по **последним позициям этого региона** (дата снимка ≈ дата на графике). Без региона — автопересчёт кэша, если в БД есть позиции новее `monitoring_data_table_columns_projects.updated_at`.

## 3.5.11-dev — 2026-05-29

- **Календарь `/monitoring/{id}`:** быстрый выбор периода — 7 / 30 / **60 / 90 / 180 / 365** дней и «прошлый месяц» (как в тренде v2).

## 3.5.10-dev — 2026-05-29

- **Пресеты графика ТОП:** убрана кнопка «3/5/10/20/30/50/100» (дублировала «Все»); сохранённый выбор `35102050100` → «Все».

## 3.5.9-dev — 2026-05-29

- **KPI `/monitoring/{id}`:** в полоске сводки добавлены **ТОП-1**, **ТОП-3**, **ТОП-100** (рядом с ТОП-10/30); колонка `top1` в снимке проекта (миграция). Значение ТОП-1 заполнится после следующего пересчёта позиций.

## 3.5.8-dev — 2026-05-29

- **Фикс:** фильтры таблицы («Динамика», «нецелевые URL») больше не попадают в шапку блока графиков — только в заголовок DataTables ключевых слов.

## 3.5.7-dev — 2026-05-29

- **`/monitoring/{id}`:** быстрый выбор серий % в ТОП (TOP-10, 3/5/10/20/30/100, 3/5/10/20/30/50/100, все) на вкладке «% в ТОП» при выбранном одном регионе. API: ТОП 3/5/10/20/30/50/100. `cabinet-monitoring-show-charts.js`.

## 3.5.6-dev — 2026-05-29

- **Русские подписи регионов:** Google-регионы в легенде графика и фильтрах — «Москва [1011969]» вместо `Moscow, Moscow, Russia` (`MonitoringLocationLabel` + `google_geo_regions.json`). Кнопка сброса зума графика — «Сбросить».

## 3.5.5-dev — 2026-05-29

- **Цвета линий графиков:** фиксированная контрастная палитра вместо `mt_rand` (синий / оранж / зелёный / фиолет / …) — «Москва» и «Воронеж» больше не сливаются. `MonitoringChartPalette`, `applyDistinctLineColors` в JS.

## 3.5.4-dev — 2026-05-29

- **Графики мониторинга:** `spanGaps` — линия соединяет разрозненные точки (пропуски = `null`); исправлен порядок данных «средняя по регионам» (`setDataForLabels`). `AreaChartData`, `/monitoring/charts`, child-chart v2.

## 3.5.3-dev — 2026-05-29

- **Графики средней позиции:** инвертированная ось Y (как SER) — улучшение к ТОП-1 идёт **вверх**, как рост % в ТОП. `/monitoring/{id}`, child-chart v2, дашборд «Средняя позиция». Хелпер `cabinet-monitoring-chart-scales.js`.

## 3.5.2-dev — 2026-05-29

- **Логика вкладок (SER):** «Обзор» — только графики с заголовком и подсказкой (что за график при всех ПС vs один регион); «Ключевые слова» — только таблица (по умолчанию). Убран дубль названия «Позиции» в подменю. Подсказки под вкладками.

**Проверка:** `/monitoring/{id}#overview` — графики без таблицы; `#keywords` — таблица без графиков.

## 3.5.1-dev — 2026-05-29

- **Локализация `/monitoring/{id}`:** `Dynamics` → «Динамика», `Selected group` → «Все группы», `YW` → «Ч», DataTables и плейсхолдеры поиска на русском.

## 3.5.0-dev — 2026-05-29

- **Страница проекта `/monitoring/{id}` — редизайн по бенчмарку SER (rankings overview/detailed) + Topvisor (dynamics):** полноэкранный shell как v2, breadcrumb, favicon, вкладки **Обзор | Позиции**, навигация модуля (конкуренты, ТОП-100, группы…), KPI-полоса из `monitoring-v2/project-stats`, единый toolbar (ПС / даты / группы), графики только в «Обзоре», таблица — ядро «Позиций». CSS `cabinet-monitoring-show.css`, JS `cabinet-monitoring-show-chrome.js`.

**Проверка:** http://localhost:3002/monitoring/88 — **v3.5.0-dev**; вкладки, KPI, таблица ключей.

## 3.4.81-dev — 2026-05-29

- **Страница проекта `/monitoring/{id}` (доработка):** убраны громоздкие KPI-плитки → компактные pill-шорткаты; дубль шапки (назад/домен) перенесён в `card-header`; лоадер таблицы — Bootstrap spinner вместо огромного `1485.gif` (пустая «шестерёнка»); ошибка загрузки таблицы с toastr.

**Проверка:** http://localhost:3002/monitoring/88 — **v3.4.81-dev**, таблица появляется без пустого серого блока.

## 3.4.80-dev — 2026-05-29

- **Страница проекта `/monitoring/{id}`:** оболочка в стиле v2 — шапка с возвратом к списку и доменом, KPI info-box вместо `small-box`, компактные фильтры, outline-кнопки действий, фон и карточка таблицы. CSS: `cabinet-monitoring-show.css`. DataTables без изменений.

**Проверка:** http://localhost:3002/monitoring/88 — badge **v3.4.80-dev**, KPI, фильтры, таблица ключевых слов.

## 3.4.79-dev — 2026-05-29

- **Список v2, колонка действий:** dropdown меню не обрезается таблицей; пункты с нормальным тёмным текстом; кнопки «Позиции» / регионы / меню выровнены по высоте; у иконки меню убрана лишняя стрелка `dropdown-toggle`.

## 3.4.78-dev — 2026-05-29

- **Список v2:** зелёный бейдж **«Гостевая ссылка»** у проекта, если у текущего пользователя есть активная публичная ссылка (таблица и карточки). Клик открывает ссылку; tooltip — срок действия. После создания/отзыва бейдж обновляется без перезагрузки страницы.

## 3.4.77-dev — 2026-05-28

- **Гостевой доступ к статистике проекта:** пункт меню **«Статистика для клиента»** в списке v2 → модалка с KPI, таблицами по регионам и блоком публичной ссылки (TTL 30/90/180/365 дн. или бессрочно). Снимок JSON в `monitoring_public_shares`, просмотр без авторизации: `GET /public/share/monitoring-v2/{token}`.

**Проверка:** http://localhost:3002/monitoring-v2 → меню проекта → «Статистика для клиента» → создать ссылку → открыть в инкогнито.

**Миграция:** `php artisan migrate --path=database/migrations/2026_05_28_120000_create_monitoring_public_shares_table.php`

## 3.4.76-dev — 2026-05-28

- **Портфель «Тренд ТОП-10»:** полоса и точки прогресса привязаны к времени (~:eta с для 87 проектов), подпись `45% · 59 / ~90 с` (раньше полоска почти не двигалась).

## 3.4.75-dev — 2026-05-28

- **Портфель «Тренд ТОП-10»:** убрана порционная загрузка (больше нет «проекты 46–60 из 87»); снимок в **localStorage** + серверный кэш до полуночи; при устаревании — предупреждение и кнопка **«Пересчитать тренд»** (`refresh=1`). Ключ кэша `v9`.

## 3.4.74-dev — 2026-05-28

- **Портфель «Тренд ТОП-10»:** кэш до **полуночи** (не 10 мин); один запрос вместо порций (повторное открытие вкладки — секунды). Первый расчёт за день по большому портфелю — по-прежнему 1–2 мин. Ключ кэша `v8`, `MONITORING_TREND_CACHE_SECONDS` в `.env` при необходимости.

## 3.4.73-dev — 2026-05-28

- **Портфель «Тренд ТОП-10»:** исправлено падение при большом портфеле (~87 проектов) — агрегация ТОП-10 в SQL вместо загрузки всех позиций в память (OOM → «Не удалось построить тренд»). Кэш тренда `v7`.

## 3.4.72-dev — 2026-05-28

- **Список v2, колонка «ПС»:** подпись расписания (ручной / время съёма) скрыта по умолчанию, показывается при наведении на иконку ПС (оверлей, без раздувания строки).

## 3.4.71-dev — 2026-05-28

- **Список v2, колонка «ПС»:** исправлен тултип при наведении — HTML в `title` ломал разметку (текст в ячейке, `">`); теперь только экранированный plain-текст, строки городов через перевод строки.

## 3.4.70-dev — 2026-05-28

- **Список v2, колонка «ПС»:** под иконкой Яндекс/Google — кратко режим и время съёма; в тултипе — по каждому городу (Москва — каждый день 09:00 и т.д.).

## 3.4.69-dev — 2026-05-28

- **Мастер создания, шаг «Расписание»:** пояснение, что пустые поля при **любом** режиме = только ручной съём; список обязательных полей по режимам.

## 3.4.68-dev — 2026-05-28

- **Мастер создания, шаг «Регионы»:** поиск городов по **русскому и английскому** через локальные справочники (`yandex_lr_regions.json`, `google_geo_regions.json`); исправлен ответ API (всегда JSON-массив, не пустая строка → Select2).

## 3.4.67-dev — 2026-05-28

- **Тренд ТОП-10:** ускорение — один SQL на 15 проектов вместо 86 отдельных; при ≥18 проектах — порционная загрузка с реальным прогрессом («База: 16–30 из 86», таймер секунд); кэш 10 мин (ключ `v6`).

## 3.4.66-dev — 2026-05-28

- **Создание проекта, шаг «Запросы»:** убран полноэкранный `please-wait` при загрузке таблицы (гигантский логотип); индикатор только у DataTables; ограничен размер лого в глобальном оверлее (`max-width: 12rem`).

## 3.4.65-dev — 2026-05-28

- **Фавиконки (прод):** ротация очереди fill (не одни и те же 3 проекта на 40 запросов); кэш неудачных попыток 6 ч; в логе `failed`/`reason`; остановка фона при `stalled` или 5 шагах без прогресса; `/monitoring-v2/favicon` — полный обход при догрузке.
- **Проверка:** badge **v3.4.65-dev**, расширенный лог — `http.favicons.fill.done` с `attempted`/`failed`; после деплоя обновить страницу (не 40 одинаковых fill подряд).

## 3.4.64-dev — 2026-05-28

- **Создание проекта:** кнопки шагов в блоке с отступами («К списку» / «Далее»), подпись «Далее»; блокировка «Далее» при сохранении.

## 3.4.63-dev — 2026-05-28

- **Список v2:** исправлен `ReferenceError: monV2DebugLine is not defined` — из‑за этого не грузились проекты и графики.

## 3.4.62-dev — 2026-05-28

- **Создание проекта:** пустой экран — у шагов был `data-bs-target` вместо `data-target` для bs-stepper (панели не получали `.active`). Исправлено; логика мастера в `cabinet-monitoring-create.js`.

## 3.4.61-dev — 2026-05-28

- **Прод / боевой:** тренд — SQL «последняя позиция за день» (быстрее на 80+ проектах), лог `http.trend.*` в расширенном логе, таймаут 5 мин.
- **Фавиконки:** фоновая догрузка не останавливается при `rebuilt: 0` (пока `pending > 0`); порция до 3–5 иконок, wall 20 с.

## 3.4.60-dev — 2026-05-28

- **Создание проекта** (`/monitoring/create`): исправлено добавление (падение JS без `#id` в URL); сохранение проекта до шага с запросами; понятные шаги, статус-баннер, тексты; ссылка на `/monitoring-v2`.

## 3.4.59-dev — 2026-05-28

- **Тренд портфеля — загрузка:** оверлей со спиннером, прогресс-баром и анимацией точек; toastr «Считаем тренд»; после ответа — поэтапная отрисовка дат с полосой «N из M».

## 3.4.58-dev — 2026-05-28

- **Аватары в списке/карточках:** только круглое фото, без квадратной рамки `img-bordered-sm`; инициалы — круглый плейсхолдер.

## 3.4.57-dev — 2026-05-28

- **Тренд портфеля:** среднее по **всем проектам с историей** на каждую дату; если в день нет съёма — **ближайшее** известное значение (меньше «пилы»). До 100 проектов; подсказка — сколько с историей; линия дорисовывается по точкам после загрузки.

## 3.4.56-dev — 2026-05-28

- **Таблица проектов:** убран DataTables FixedHeader — при скролле страницы больше нет «полосы», перекрывающей первую строку.

## 3.4.55-dev — 2026-05-28

- **Тренд портфеля:** расчёт по проектам порциями (без одного тяжёлого запроса на 80+ проектов), лимит 35 проектов, таймаут 180 с; при сбое — предупреждение, не «красный» 500.

## 3.4.54-dev — 2026-05-28

- **Портфель «Тренд ТОП-10»:** исправлен расчёт (даты позиций, группировка как у графика региона); при переключении вкладки сразу грузится линейный график; ошибки/пустые данные — подсказка и toastr.
- **Проверка:** вкладка «Тренд ТОП-10», период в «Настройка графиков» — линия 0–100%, не остаётся график «Лидеры».

## 3.4.53-dev — 2026-05-28

- **Тултип ПС:** шире, строки города без переноса внутри (`white-space: nowrap`).

## 3.4.52-dev — 2026-05-28

- **График региона:** снова локальные контролы — период, группировка, метрика, серии ТОП (по умолчанию = «Настройка графиков»; можно переопределить для одного региона, ссылка «Как в общих настройках»).
- **Проверка:** раскрыть регион → «Показать график» — селекты над графиком; смена периода не трогает глобальные настройки.

## 3.4.51-dev — 2026-05-28

- **Тултип ПС:** города списком **с новой строки** (не через запятую в одну линию).

## 3.4.50-dev — 2026-05-28

- **Графики % в ТОП** — ось Y фиксирована **0–100%** (раньше сжималась к диапазону данных ~72–80%). Средняя позиция — по-прежнему авто-масштаб.
- **Проверка:** child-rows и портфель «Тренд ТОП-10» — шкала от 0 до 100.

## 3.4.49-dev — 2026-05-28

- **Колонка ПС:** при наведении на иконку Яндекс/Google — тултип со списком подключённых городов (`engine_regions` в `listProjects`, Bootstrap tooltip).
- **Проверка:** http://localhost:3002/monitoring-v2 — навести на «Я» / «G» в строке проекта с несколькими регионами.

## 3.4.48-dev — 2026-05-28

- **Настройка графиков: серии ТОП** — вместо «5/10/20»: **3/5/10/20/100** и **3/5/10/20/50/100** (+ TOP-10, «Все ТОП»). Старый пресет `51020` → первый набор.

## 3.4.47-dev — 2026-05-28

- **График регионов: серия TOP-10** — фильтр по номеру ТОП (раньше «ТОП-10» матчил ТОП-100, ТОП-30… — в тултипе были все серии).

## 3.4.46-dev — 2026-05-28

- **Child-rows кэш p7** — сброс устаревшего HTML в плитках (старые «Диапазон дат» / бейджи Region code). Клиентский кэш с меткой `p7`. Убраны бейджи Region code/ID (код региона в заголовке `[193]`).

## 3.4.45-dev — 2026-05-28

- **Настройка графиков** — в навбаре рядом с «Скрыть портфель»: период (30–365), группировка (по умолчанию **недели**), метрика (% / средняя позиция), серии ТОП. Defaults: **90 дней**, недели. `localStorage`. У регионов убраны дублирующие контролы.

## 3.4.44-dev — 2026-05-28

- **Портфель: тренд ТОП-10** — вкладка «Тренд ТОП-10», кнопки периода **30 / 60 / 90 / 180 / 365** дней; `POST /monitoring-v2/portfolio/top10-trend` (средний % по проектам списка, кэш 5 мин). У региона в child-rows — быстрые кнопки тех же периодов для календаря.

## 3.4.43-dev — 2026-05-28

- **График регионов** — данные с `/monitoring/charts` (как в классическом мониторинге): календарь «Диапазон дат», группировка дням/неделям/месяцам; линии только сплошные, без пунктира. Кэш child-rows `p6`.

## 3.4.42-dev — 2026-05-28

- **Плитки: регионы** — при раскрытии карточка на всю ширину сетки; таблица в горизонтальном скролле; график регионов в плитках; `overflow` не режет колонки ТОП.

## 3.4.41-dev — 2026-05-28

- **Портфель позиций: скрыть/показать** — кнопка в навбаре рядом с «Проекты»; состояние в `localStorage` (`cabinet-mon-v2-portfolio-visible`).

## 3.4.40-dev — 2026-05-28

- **График регионов: срезы и серии** — выбор «Срезы» (все / текущий+1 мес. / …), метрика «% в ТОП» или «Средняя позиция», пресеты TOP-10 / 5·10·20 / все. Ось Y — авто по видимым данным (линии не сливаются). Яркие цвета, штрихи у разных ТОП. Кэш `p5`.

## 3.4.39-dev — 2026-05-28

- **Регионы (+): график ТОП** — под таблицей ПС кнопка «Показать график»: Chart.js, линии по срезам (TOP-1…100), по умолчанию виден TOP-10, остальные — в легенде. Палитра как у дашборда v2. Кэш child-rows `p4`.

**Проверка:** http://localhost:3002/monitoring-v2 → «+» у проекта → «Показать график» под таблицей Яндекса.

## 3.4.38-dev — 2026-05-28

- **Регионы (+): подписи периодов** — под датой: «текущий срез», «≈1 мес. назад», «≈3 мес.» …; при наведении на ТОП — «Изменение к срезу 30.04.2026 (≈1 мес.)». Кэш child-rows `p3`.

## 3.4.37-dev — 2026-05-28

- **Регионы (+): дельта ТОП** — в скобках сравнение с **предыдущей датой в таблице** (30.04 → 28.05), а не первая/вторая проверка внутри одного месяца. Строки по `latest_created` сверху вниз; у самой старой даты скобок нет. Кэш child-rows `p2`.

**Проверка:** 1-diesel.ru, Воронеж — 28.05.2026 TOP-10 **69.57 (-5.65)** относительно 30.04 **75.22**.

## 3.4.36-dev — 2026-05-28

- **Фавиконки без «забыли CABINET_STORAGE_URL»:** рантайм **не** тянет PNG с lk по `.env`. Одноразово на сервере: `php artisan monitoring:import-favicons-from-legacy --from=https://lk.redbox.su` (есть `--dry-run`, `--limit`). Дальше только локальный `storage` + `favicons/fill` / `monitoring:refresh-favicons --missing`.

## 3.4.35-dev — 2026-05-28

- **Фавиконки на cabinet.datagon.ru (403):** `<img>` через `GET /monitoring-v2/favicon?project=` (PHP `response()->file`), не `/storage/monitoring-favicons/*.png`. При ошибке img — повтор с `&refresh=1`. Кэш списка `s15`.

**Проверка на проде:** F5 → `GET /monitoring-v2/favicon?project=…` **200**; `storage:link`. Миграция PNG: `sudo -u cabinet_data_usr /opt/php74/bin/php artisan monitoring:import-favicons-from-legacy --from=https://lk.redbox.su` (не голый `php` от root — на s3 это PHP 8.x и fatal Laravel 6).

## 3.4.34-dev — 2026-05-28

- **Child-rows быстрее:** SQL только по месяцам среза (0/1/3/6/12), не 12 календарных месяцев; одна выборка `monitoring_keyword_prices` на все ПС вместо запроса на каждый месяц×ПС. На фронте — общий кэш HTML, прогрев первых 6 проектов после списка, prefetch по наведению на строку (120 ms).

## 3.4.33-dev — 2026-05-28

- **Пустая таблица / нет лога админа:** в `cabinet-monitoring-v2-list.js` не было закрывающей `}` у `initDataTable()` — синтаксическая ошибка, весь скрипт не выполнялся (список не грузился, панель лога не показывалась). Панель лога — видна сразу для админа (без `display: none`). Debugbar отключён на `GET /monitoring-v2` (раньше только `monitoring-v2/*` AJAX).

## 3.4.32-dev — 2026-05-28

- **«+» регионы:** без лишнего `COUNT` по БД для типичных проектов (≤12 ПС, ≤8000 слов); prefetch при наведении на «+»; строка «Загрузка статистики по регионам…» при раскрытии. Повторное открытие — из кэша (~150 ms).

## 3.4.31-dev — 2026-05-28

- Слабая иконка: PNG &lt; 1100 B (микрозаглушка gstatic) — снова в очередь `favicons/fill` (`needsRefresh`).

## 3.4.30-dev — 2026-05-28

- **1-diesel.ru и WP:** парсинг `<link rel=icon href=...>` без кавычек; поиск `favicon.ico` в HTML; для главной сначала браузерный UA (YandexBot часто таймаутит).

## 3.4.29-dev — 2026-05-28

- **favicon.ico 16×16** (kursk.himopttorg.ru и др.): `IcoFileService::extractIcon(128)` возвращал `false` — брали заглушку Google; декод с размеров 128→16, затем upscale в PNG 128×128.

## 3.4.28-dev — 2026-05-28

- **redbox.su и похожие:** второй HTTP-проход с браузерным UA; конструктор `ProjectFaviconFetcher` без type-hint `Client` (Laravel иначе подставлял Guzzle с `http_errors=true` — gstatic 404+PNG не читался). «Буквенный» stub (~1.3 KB) по-прежнему не показываем.

## 3.4.27-dev — 2026-05-28

- **Поиск в таблице:** `searching: true` (без поля DT в DOM) — иначе при `searching: false` DataTables не вызывает `ext.search` при `draw()`.

## 3.4.26-dev — 2026-05-28

- **Поиск v2:** фильтр таблицы привязан к `settings` DataTable (не `nTable.id`); debounce 220 ms; график обновляется без пересоздания (`chart.update('none')`); поиск по URL, имени и пользователям проекта.

## 3.4.25-dev — 2026-05-28

- **502 на `projects/list`:** Laravel Debugbar с `storage.enabled=false` писал профиль AJAX в заголовки (`phpdebugbar-*`) → nginx `upstream sent too big header`. Исключение `monitoring-v2/*` в `config/debugbar.php`, storage по умолчанию в файл; запас: `fastcgi_buffer_*` в `scripts/nginx-cabinet.local.conf` (перезапуск `dev-local.sh`).

## 3.4.24-dev — 2026-05-28

- Исправлен **502** на `projects/list`: убран N+1 `findDonorProject` при сборке JSON; `propagate` в try/catch, без сброса кэша внутри `remember`.

## 3.4.23-dev — 2026-05-28

- **favicon.ico:** декод через `lordelph/icofileloader` (16×16 ICO → PNG), не картинки из `/upload/iblock`.
- Порядок: `/favicon.ico` → `<link rel="icon">` → apple-touch; без og:image и баннеров.
- Донор по хосту: не «самый большой файл», а нормальная иконка (штраф &gt; 20 KB).
- Кэш списка: **s14**.

## 3.4.22-dev — 2026-05-28

- **metprof-vrn.ru и .ico:** GD не читает `.ico` — декод через PNG внутри ICO / Imagick; HTML главной снова парсится (`fetchHtml`, не лимит 12 KB).
- Порядок URL: первый **пригодный** PNG по списку (gstatic до Google s2 с буквой).

## 3.4.21-dev — 2026-05-28

- Загрузка фавиконок: **User-Agent YandexBot/3.0** (env `MONITORING_FAVICON_USER_AGENT`) — сайты с антиботом чаще отдают HTML и `/favicon.ico`.

## 3.4.20-dev — 2026-05-28

- Загрузчик фавиконок: **сначала сайт** (favicon.ico, HTML), агрегаторы (Google) — запасной; Google 128×128 с буквой больше не перебивает реальный .ico.
- Порог мелкого файла **500 B** (gstatic 16×16); буквенная плитка Google по-прежнему 1290–1345 B.
- Клик «обновить иконку» — полный `favicon_src_project_id` в `updates` для клиента.

## 3.4.19-dev — 2026-05-28

- Порог «слабой» иконки: не «&lt; 3 KB», а **буквенная заглушка Google ~1.3 KB** (1290–1345) или **&lt; 800 B**. Нормальные 1.1–2.5 KB снова показываются (jetour, proktolog, eda-doma и т.д.).
- Кэш списка: **s11**.

## 3.4.18-dev — 2026-05-28

- **Заглушки Google (~1.3 KB)** больше не считаются нормальной иконкой: донор выбирается по **размеру PNG**, не по дате; слабые файлы копируются с проекта того же домена (напр. `metplus-msk.ru` id 672 → 82).
- Быстрый `favicons/fill` **не сохраняет** буквенные PNG; для слабых — полный обход сайта.
- Кэш списка: **s10**.

## 3.4.17-dev — 2026-05-28

- **Иконки в списке:** `<img src>` — **`/storage/monitoring-favicons/{id}.png`** (статика, без auth). Маршрут `/monitoring-v2/favicon?project=` давал **302** для подзапроса картинки → буква-заглушка при `missing_favicons: 0`.
- Кэш списка: **s9**.

**Проверка:** Network → `GET /storage/monitoring-favicons/82.png` **200**; в шапке **v3.4.17-dev**.

## 3.4.16-dev — 2026-05-28

- **Иконки в таблице:** в JSON — `favicon_src_project_id` + `favicon_v`; `<img src>` собирается на клиенте из **относительного** `/monitoring-v2/favicon?project=…` (нет рассинхрона `127.0.0.1` vs `localhost`).
- Дубли домена: тот же `favicon_src_project_id`, что у донора с PNG.
- Убрана полная перерисовка DataTable после каждого `favicons/fill` — только patch строк и DOM иконок.
- Кэш списка: суффикс **s8**.

**Проверка:** http://127.0.0.1:3002/monitoring-v2 → F5 → у двух `metplus-msk.ru` одна и та же картинка; Network — `GET /monitoring-v2/favicon?project=…` **200**.

## 3.4.15-dev — 2026-05-28

- `favicon_url` в list — через `/monitoring-v2/favicon?project=` (тот же хост, что страница), не `asset(storage/…)` с другим origin.

## 3.4.14-dev — 2026-05-28

- **«Обновить»:** в `list` сразу `favicon_url` с донора того же домена; перерисовка таблицы; фоновая догрузка только для строк без иконки; прогресс «Иконки проектов».

## 3.4.13-dev — 2026-05-28

- Исправлен **500** на `favicons/fill?project_id=` — `isNewerFavicon` падал на строковом `favicon_updated_at` без cast Carbon.

## 3.4.12-dev — 2026-05-28

- **Один домен — одна иконка:** при `list` и `favicons/fill` копирование PNG с любого проекта того же хоста в портфеле; кэш списка `s5`.

## 3.4.11-dev — 2026-05-28

- **Клик по иконке** — `POST favicons/fill` с `project_id`, полная загрузка; дубли URL копируют PNG с соседнего проекта.
- **«Обновить»** — после метрик запускает догрузку пропущенных фавиконок; сброс кэша списка при сохранении иконки.

## 3.4.10-dev — 2026-05-28

- **Зависание при F5:** в таблице больше нет `favicon?host=` в `<img>` (десятки on-demand fetch); буква-заглушка → фон `favicons/fill`. `?host=` отключён.

## 3.4.9-dev — 2026-05-28

- `favicons.fill`: **2 иконки за запрос**, быстрый режим (Google/gstatic, без HTML), лимит 12 с; повтор при обрыве.

## 3.4.8-dev — 2026-05-28

- `snapshots.fill`: **1 проект за запрос**, лимит ~22 с на HTTP (без обрыва на 90 с); повтор при `status: 0`; тост при таймауте.

## 3.4.7-dev — 2026-05-28

- Очередь фоновых задач: фавиконки только после `list.ready` и завершения `snapshots.fill`; отмена отложенного таймера при новом list.
- Старый кэш списка (без `snapshots_pending`) — суффикс кэша `s4`; на клиенте пересчёт pending по пустым строкам.
- Админ-лог: новая сессия и `MonitoringV2DebugLog::clear` при открытии страницы.

## 3.4.6-dev — 2026-05-28

- Исправлено «страница не открывается» / зависание вкладки: метрики и фавиконки **последовательно** (не параллельно), лимит шагов fill, лёгкое обновление строк таблицы без полной пересборки на каждую порцию.

## 3.4.5-dev — 2026-05-28

- Админ-лог (Super Admin / admin): панель «Расширенный лог» внизу `/monitoring-v2`, серверные тайминги list/fill + клиентские события; `debug_session` в POST.
- Фавиконки без сохранённого файла: сначала `?host=`, меньше 404 в консоли на `?project=`.

## 3.4.4-dev — 2026-05-28

- Фавиконки: фоновая догрузка `POST /monitoring-v2/favicons/fill` (6 проектов за запрос), при ошибке img — повтор с `refresh=1` и fallback по `host`; в fetcher приоритет Google/gstatic.

## 3.4.3-dev — 2026-05-28

- После фоновой догрузки метрик таблица **перерисовывается целиком** (DataTables не обновлял HTML ячеек при `row().data()`).
- `pending` в fill считается по фактически пустым снимкам в БД, не «остаток очереди».

## 3.4.2-dev — 2026-05-28

- Исправлено зависание «Загрузка списка…»: **list отдаётся сразу** (без пересчёта снимков в том же HTTP).
- Метрики догружаются фоном: `POST /monitoring-v2/snapshots/fill` (3 проекта за запрос, прогресс «Метрики проектов: N / M»).
- «Обновить» — сброс кэша списка + фоновый fill с `force=1` (5 проектов за запрос).

## 3.4.1-dev — 2026-05-28

- ~~Пересчёт снимков внутри list~~ — убрано в 3.4.2 (висло на 86 проектах).

## 3.4-dev — 2026-05-27

- Таблица v2: колонка **ТОП 3** (и опционально ТОП 5); меню **«Столбцы»** рядом с переключателем вида — включение/скрытие метрик.
- Настройки столбцов **на пользователя** в БД (`monitoring_v2_user_preferences.list_columns`), `POST /monitoring-v2/preferences/list-columns`.
- Миграция: `2026_05_27_231500_create_monitoring_v2_user_preferences_table.php`.

**Проверка:** http://localhost:3002/monitoring-v2 → «Столбцы» → снять/включить ТОП 30, обновить страницу — выбор сохранён. Badge `v3.4-dev`.

## 3.3.3-dev — 2026-05-27

- Исправлено зависание: автопересчёт снимков убран с обычной загрузки (только по кнопке «Обновить» с `rebuild_snapshots`). Список 86 проектов отдаётся сразу.

## 3.3.2-dev — 2026-05-27

- Пустые ТОП/позиция/слова: v2 читает снимок `monitoring_data_table_columns_projects`; при отсутствии — автопересчёт (до 40 проектов за запрос). Кнопка «Обновить» шлёт `rebuild_snapshots=1`. CLI: `php artisan monitoring:refresh-snapshots --all`.

## 3.3.1-dev — 2026-05-27

- Выделение строк таблицы и плиток: фон + полоска слева (исправлен конфликт с zebra CSS).

## 3.3-dev — 2026-05-27

- Фавиконки проектов: поля `favicon_path`, `favicon_host`, `favicon_updated_at`; PNG **128×128** в `storage/app/public/monitoring-favicons/{id}.png`; выбор лучшего источника (HTML + apple-touch + агрегаторы); сохранение при создании/смене URL.
- API списка: `favicon_url`; `GET /monitoring-v2/favicon?project={id}`.
- Прогон существующих: `php artisan monitoring:refresh-favicons --missing` (после миграции БД).

## 3.2.3-dev — 2026-05-27

- ТОП 10/30/100: наполнение ячейки по проценту (цветная полоса слева направо), читаемее доля в ТОП.

## 3.2.2-dev — 2026-05-27

- Колонка ПС: компактные иконки (как в классическом мониторинге), без красных «плашек» на весь ряд.

## 3.2.1-dev — 2026-05-27

- Фавиконки проектов: `GET /monitoring-v2/favicon?host=…` — цепочка источников (apple-touch, icon.horse, Google 128px), нормализация в PNG 64×64, кэш в `storage/app/monitoring-favicons/`.

## 3.2-dev — 2026-05-27

- ТОП в таблице: три отдельные колонки (10 / 30 / 100) вместо одной «пилюли» с серыми метками; цветовая полоска и фон по силе показателя (как Topvisor).

## 3.1.3-dev — 2026-05-27

- Колонка **ПС**: вместо бледных `badge bg-light` — цветные иконки Яндекс / Google (читаемые на светлой строке).

## 3.1.2-dev — 2026-05-27

- Ячейка проекта: фавиконка домена (Google s2, fallback на букву), читаемые цвета ссылки (домен серый, название тёмное).

## 3.1.1-dev — 2026-05-27

- Контраст UI: тёмная шапка таблицы, жирные ТОП 10/30/100, насыщенные дельты, иконки проектов на акцентном фоне, тени у панелей (меньше «тусклого серого»).

## 3.1-dev — 2026-05-27

**Полная перекомпоновка страницы** (не «виджет в карточке»):

- **Серый фон** и `padding: 0` у body карточки — два отдельных блока на всю ширину.
- **Блок «Портфель позиций»** — 5 KPI в ряд + крупный график (режимы Лидеры / Распределение / Средние ТОП).
- **Блок «Все проекты»** — свой заголовок, toolbar, **таблица с фиксированным thead** (Проект · ТОП 10/30/100 · Позиция · … · Действия), без вложенной card-in-card.
- Метка **v3.1-dev** в правом верхнем углу страницы (`data-ui-build="3.1"`).

Удалены: серая «простыня» lead, дублирующий поиск DataTables.

**Проверка:** Cmd+Shift+R → виден бейдж `v3.1-dev`, серый фон, две белые панели.

## 3.0-dev — 2026-05-27

Редизайн `/monitoring-v2` по бенчмаркам [SE Ranking](./cabinet-monitoring-benchmark-seranking.md) и [Topvisor](./cabinet-monitoring-benchmark-topvisor.md). Сводка: [cabinet-monitoring-benchmark-comparison.md](./cabinet-monitoring-benchmark-comparison.md).

**Портфель (SER):** один крупный график с режимами «Лидеры / Распределение / Средние ТОП»; боковая колонка KPI (проекты, ср. ТОП‑10, ср. позиция, слова, слабые); переключатель метрик лидеров (ТОП‑10 / ТОП‑30 / позиция).

**Таблица (Topvisor + SER):** колонка «Проект» (иконка + домен + ссылка); **ТОП 10/30/100** в одной ячейке с дельтами; ср. позиция; быстрые кнопки «Позиции» и «Регионы»; меню действий.

**Плитки:** крупные KPI (позиция, ТОП‑10, слова), полоска ТОП с дельтами, те же быстрые действия; сетка до 3 колонок на широком экране.

**Прочее:** кнопка «Обновить» (сброс кэша списка); бейдж числа выбранных; дельты `diff_top*` в JSON; исправлен зависший режим плиток (2.9.1).

**Проверка:** http://localhost:3002/monitoring-v2 — Cmd+Shift+R.

## 2.9.1-dev — 2026-05-27

- **Исправлено:** режим «Плитки» зависал на «Загрузка 0 / N» — в `ingestAll` остались ссылки на удалённые переменные (`aggregateWords` и др.), в strict mode падал JS до отрисовки карточек.

**Проверка:** `/monitoring-v2` → «Плитки» → список доходит до N/N, графики заполняются.

## 2.9-dev — 2026-05-27

- UX по бенчмаркам SER/Topvisor: **таблица по умолчанию**; на карточках — компактная полоска **ТОП 10/30/100**; на дашборде — переключатель метрик графика лидеров (ТОП‑10, ТОП‑30, средняя позиция).
- Тексты подсказок в `ru.json` обновлены под логику «хаб = дайджест, детали в проекте».

**Проверка:** http://localhost:3002/monitoring-v2 → таблица; плитки → полоска ТОП; дашборд → кнопки метрик.

## 2.8-dev — 2026-05-27

- Вместо блока «86 / 86 загружено» — **дашборд Chart.js**: распределение по ТОП‑10, топ‑10 проектов, средние ТОП3/5/10/30; 4 цифры (проекты, средний ТОП‑10, слова, слабые &lt;30%). Графики пересчитываются при поиске/фильтре.

## 2.7-dev — 2026-05-27

- Палитра v2 приведена к `--cabinet-primary`: убран яркий `btn-info` (бирюза) у меню карточки, приглушены KPI/метрики/дельты ТОП, блоки регионов без кислотного `card-success`.

## 2.6-dev — 2026-05-27

- Переключатель **«Плитки / Таблица»** на `/monitoring-v2`: таблица DataTables на том же JSON, что карточки — сортировка по ТОП‑10, 30, словам и др.; режим в `localStorage`.
- Раскрытие регионов в таблице — как в классике (`+` / child-rows).

**Проверка:** http://localhost:3002/monitoring-v2 → «Таблица» → клик по заголовку «10 %»; повторно «Плитки».

- Исправлено: падение `tableFilterFn` при первом открытии таблицы (фильтр вызывался до готовности `dataTable`).

## 2.5-dev — 2026-05-27

- **Регионы и дни:** `MonitoringChildRowsService` — до 5 SQL по месяцам (или один при ≤120k строк), кэш HTML 10 мин; повторное раскрытие без запроса (кэш в DOM).
- **Mastered:** предзагрузка `monitoring_keyword_prices` одним запросом (раньше тысячи `value()` на каждую позицию — основная причина 30–50 с на child-rows).
- Консоль: `[monitoring-v2] child-rows <id> … ms` для замера.

**Проверка:** `/monitoring-v2` → «Регионы и дни» на проекте с несколькими регионами; Network — один `child-rows/get`; второй клик (свернуть/развернуть) — без запроса.

## 2.4-dev — 2026-05-27

- Список v2 быстрее: права меню считаются **один раз** (без 86× `apply_team_permissions` / `can()`), кэш JSON 120 с (ключ: пользователь + версия + штамп снимков), сброс при обновлении снимка проекта.
- Клиент: карточки рисуются пакетами по 18 кадров; аватары — инициалы до загрузки, фото с `loading="lazy"` (исправлено: `d-none` + IntersectionObserver не подгружали картинки).
- `POST …/list?refresh=1` — принудительный сброс кэша.

**Проверка:** Network → только `monitoring-v2/projects/list` (не фильтр `users`); консоль `[monitoring-v2] list loaded in … ms`; повторное F5 &lt; 500 ms при горячем кэше.

## 2.3-dev — 2026-05-27

- Список v2: `MonitoringProjectListSerializer` — один SQL + снимки, **без 172 рендеров Blade** (было ~35 с на 86 проектах). Меню и аватары собираются в JSON и на клиенте.
- Исправлен вывод «Освоено»: процент рендерится как HTML, не как текст тегов.

## 2.2-dev — 2026-05-27

- Исправлена загрузка v2: один запрос `POST /monitoring-v2/projects/list`, метрики из кэша БД (без 30× тяжёлого `ProjectData` в одном HTTP).
- При ошибке — красный блок с текстом на русском.
- Архитектура снимков: [cabinet-monitoring-snapshots.md](./cabinet-monitoring-snapshots.md), сервис `MonitoringProjectSnapshotService`, исправлен ночной cron (раньше передавался `Collection` в `ProjectData` — снимок не обновлялся).

## 2.1-dev — 2026-05-27

- **`/monitoring-v2`** — карточный интерфейс (сетка проектов, KPI, поиск, фильтр статусов, раскрытие регионов), без DataTables. Классический `/monitoring` без изменений.
- JS: `public/js/cabinet-monitoring-v2-list.js`, стили `cabinet-monitoring-v2.css`.
- Загрузка списка: пакетами по 30 проектов (3–4 запроса вместо 86); `getProjects` считает метрики для всех проектов в пакете.

### Проверка

1. http://127.0.0.1:3002/monitoring-v2 — карточки, «Регионы и дни», создание/удаление.
2. http://127.0.0.1:3002/monitoring — таблица как раньше.

## 2.0-dev — 2026-05-27

- Пункт бокового меню **«Мониторинг позиций (новый)»** сразу под классическим мониторингом (`main_projects`, миграция `2026_05_27_190000_add_monitoring_v2_main_project_menu`).
- Страница **`/monitoring-v2`** — экспериментальный список проектов (тот же API и DataTables, что `/monitoring`).
- Вёрстка: KPI, toolbar в карточках, столбцы в dropdown, `cabinet-monitoring-v2.css`.
- Классический интерфейс **`/monitoring`** не менялся.
- Блок уроков/описания на v2 — тот же, что на `/monitoring` (`DescriptionComposer`: alias `monitoring-v2` → `monitoring`).
- Интерфейс `/monitoring-v2` — локаль `ru`, ключи в `ru.json`, DataTables и toastr на русском.

### Проверка

1. http://127.0.0.1:3002/monitoring-v2 — список проектов, создание, скрытие столбцов; сверху блок «Урок — Мониторинг позиций».
2. http://127.0.0.1:3002/monitoring — без изменений.
