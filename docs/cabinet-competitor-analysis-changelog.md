# Анализ конкурентов — журнал версий

Badge в шапке карточки: **vX.Y** или **vX.Ys** (stable). Config: `config/cabinet-competitor-analysis.php`.  
Проверка: http://localhost:3002/competitor-analysis

## 2.9.1s — 2026-05-25 (stable)

- **Зафиксирована стабильная база** после 2.9.1: демо datagon.ru (Яндекс + Google, геозависимость, ТОП 30/20/10), геоблоки по ПС, Google только города, порог геонезависимости 40%.
- Следующие эксперименты — **2.9.2** или **2.9.1s-dev**, не правки поверх stable без bump.
- **Проверка:** badge **v2.9.1s** в шапке «Анализ конкурентов».

## 2.9.1 — 2026-05-25

- **Демо (datagon.ru):** Яндекс + Google, сравнение двух городов с расчётом геозависимости (тот же `CompetitorGeoDependency`). В UI — глубина ТОП 30/20/10 и карточки возможностей кабинета.

## 2.9.0 — 2026-05-25

- **Демо для datagon.ru:** `POST /api/demo/analiz-konkurentov/run` — ТОП-10 Яндекса по одной фразе и региону (без разбора страниц). Лимит 3 запуска/сутки, guest cookie как у анализа текста.

## 2.8.9 — 2026-05-25

- **Геозависимость при Яндекс + Google:** отдельные блоки и таблицы по каждой ПС (сравнение только Москва ↔ Воронеж внутри Яндекса и внутри Google). Общий «средний %» по всем четырём регионам убран. В колонках пар — «Москва ↔ Воронеж», без префикса ПС.

## 2.8.8 — 2026-05-25

- **Google, регионы:** в селекте только **города** (Criteria Type = City). Области/регионы вроде «Москва [20950]» убраны из `google_geo_regions.json`; для Москвы — **1011969**. Пересборка: `python3 scripts/build-google-geo-regions.py`.

## 2.8.7 — 2026-05-25

- **Геозависимость:** порог «геонезависимый» снижен с **60%** до **40%** совпадения топ-URL (`geo_independent_min_overlap`). Запрос с 46% (как «отоскоп каве») попадает в геонезависимые, а не «частично».

## 2.8.6 — 2026-05-25

- **Сравнение городов / вкладки регионов:** исправлена регрессия — результат анализа не попадал в `window.competitorResultBundle`, из‑за этого не показывались вкладки «Яндекс · Москва | Воронеж» и голубая панель «Сравнение городов» в блоке «Топ сайтов».

## 2.8.5 — 2026-05-25

- **Глубина выдачи:** в форме анализа добавлен **топ 30** (по умолчанию, подпись «рекомендуемый»); порядок опций: 30 → 20 → 10. Валидация API, XML `groupby=30`, колонки мета-таблицы 11–30, порог рекомендаций `count_repeat_top_30` (настройки модуля).

## 2.8.4 — 2026-05-25

- **Сравнение городов в SERP:** панель перенесена под заголовок «Топ сайтов» (над кнопками подсветки); список городов берётся из `byRegion`, если в `regions` один элемент.

## 2.8.3 — 2026-05-25

- **Локализация:** все подписи геозависимости и сравнения городов в SERP — на русском (`resources/lang/ru.json`).

## 2.8.2 — 2026-05-25

- **SERP, сравнение городов:** заметная голубая панель над кнопками подсветки — «Показать Воронеж» и т.д. (вместо спрятанного select). Список регионов также из `byRegion`, если в ответе нет `regions`.

## 2.8.1 — 2026-05-25

- **Геозависимость:** вместо Jaccard по объединению — **средняя доля общих URL в топе каждого города** (7 из 15 в обоих ≈ 47%, а не 30%). В ячейке пары: «47% / 47%» по регионам. Пороги: геонезависимый ≥60%, геозависимый ≤35%.

## 2.8.0 — 2026-05-25

- **SERP «Топ сайтов»:** селект «Сравнить с городом» — при выборе второго региона в каждой колонке фразы две выдачи рядом (основной регион вкладки + выбранный). Подсветка одинаковых URL/доменов работает **между городами** в одной колонке запроса.

## 2.7.9 — 2026-05-25

- **Геозависимость:** под таблицей раскрывающийся блок «Исключённые из расчёта домены» — полный список (настройки «Агрегаторы» + дефолт маркетплейсов).

## 2.7.8 — 2026-05-25

- **Геозависимость, таблица:** для каждой пары регионов отдельная колонка (Москва ↔ Воронеж и т.д.) — % совпадения, счётчики топа и список общих URL; при 3+ регионах колонок пар больше (C(n,2)).

## 2.7.7 — 2026-05-24

- **Геозависимость:** при сравнении топ-URL между городами не учитываются агрегаторы и маркетплейсы (список из настроек модуля + дефолт в config). Запросы, где остались только такие домены, помечаются «не оценён».

## 2.7.6 — 2026-05-24

- **Повторный анализ (10→20):** сброс terminal-cache в `startProgressBar`, последовательный запуск (сначала progress=1%, потом job) — UI больше не подхватывает старый результат с 10 URL и пустыми колонками 11–20.
- **Мета-таблица:** колонки 11–20 показываются только если в данных реально ≥11 позиций (`syncMetaTableColumns`).
- **Рекомендации:** порог слов считается по фактическому числу URL в выдаче (не по выбранному топ-20 при данных топ-10); для одиночных фраз — мягче доля (20%).

## 2.7.5 — 2026-05-24

- **Геозависимость:** блок снова виден при 2+ регионах (не скрывался при `clearCompetitorResults` / первом `renderCompetitorRegion`).
- **Глазик в «Анализ по страницам»:** инициализация Bootstrap Dropdown после DataTables, `strategy: fixed` (меню не обрезается).

## 2.7.4 — 2026-05-24

- **Подсветка SERP:** при наведении на совпадение остаются яркими все строки с тем же URL/доменом, остальные подсветки приглушаются.

## 2.7.3 — 2026-05-24

- **Прогресс:** при 2 регионах × 4 фразах XML больше не «застывает» на 1% — линейная шкала (≈15% на все XML-запросы, ≈80% на загрузку сайтов).

## 2.7.2 — 2026-05-24

- **Подсветка URL/доменов:** палитра из 12 различимых цветов (фон + полоска); исправлен CSS `!important`, который раньше красил все совпадения в один синий.

## 2.7.1 — 2026-05-24

- **Мета-таблица:** «Страница не загрузилась» (`fetch_failed`) — жёлтая ячейка, как пустой meta / предупреждение (раньше серая).

## 2.7 — 2026-05-24

- **Геозависимость:** при 2+ регионах блок перед «Топ сайтов» — общий вывод (геозависимые / геонезависимые / частично) и таблица по каждому запросу (Jaccard топ-URL). Сервис `CompetitorGeoDependency`, поле `geoDependency` в результате.

## 2.6.2 — 2026-05-24

- **Подсветка SERP (несколько городов / вкладки):** только URL и домены, повторяющиеся в **2+ колонках** текущего региона; безопасный поиск строк (без поломанного CSS-селектора); tooltips только на заголовках фраз и кнопках toolbar (не «отоскоп» на кнопке подсветки).
- **Вкладки регионов:** при переключении Москва/Воронеж больше не скроллит вниз к рекомендациям (скролл только по кнопке «Сформировать рекомендации»).

## 2.6.1 — 2026-05-24

- **Рекомендации:** подсказки на чипах слов (Bootstrap tooltip) — частота у конкурентов; зелёные — «есть в запросе группы».
- **2+ региона / зависание на ~49%:** php-fpm `max_execution_time` был 300 с — второй регион обрывался (~2 мин на первый город). Локально: **1200 с** в `scripts/php-fpm-cabinet.conf` и `nginx-cabinet.local.conf`; `job_max_execution_sec` в config.
- XML hybrid: тики прогресса на каждой попытке; логи `region.xml.phrase.start` / `done`.

## 2.6 — 2026-05-24

- **Несколько регионов:** прогресс больше не прыгает на ~93% после первого города и не «замирает» на втором (раньше post-scan ставил 93%, а XML/scan второго региона отклонялись как regression).

## 2.5 — 2026-05-24

- **Мета-таблица:** не все «пустые» ячейки = «защищён». Статусы: `ok`, `meta_empty` (жёлтый), `fetch_failed` (серый), `blocked` (оранжевый).
- Парсинг: og:title/description, исправлена кодировка HTML, отделение body от заголовков curl; при parallel — fallback на повтор с другим UA.
- curl не отбрасывает тело при HTTP 4xx, если HTML достаточно большой.
- **Анализ по страницам:** фразы под «глазиком» — Bootstrap dropdown у кнопки (не collapse в ячейке); колонки фраз/повторов по центру по вертикали.

## 2.4 — 2026-05-24

- **Рекомендации пересобраны:** авто-группы запросов по пересечению URL в SERP (Jaccard ≥35% или ≥3 общих URL); по каждой группе — слова в title/h1/description.
- Убран dual listbox и модалка; блок под «Анализ тегов», кнопка «Сформировать рекомендации», фильтр тегов.
- Сервис `App\Services\Competitor\CompetitorMetaRecommendations`; API `POST /get-recommendations` принимает `analysedSites` + `totalMetaTags`.

## 2.3 — 2026-05-24

- **Скорость:** параллельная загрузка страниц (`curl_multi`, по умолчанию 8 потоков) — этап 16–88% ускоряется в несколько раз.
- Пропуск URL без HTML (yandex `/images`, google `/search`).
- Реже пишем % в БД (`COMPETITOR_ANALYSIS_PROGRESS_EVERY`).
- Env: `COMPETITOR_ANALYSIS_SITE_PARALLEL`, `SITE_CONCURRENCY`, `SITE_TIMEOUT`, `SITE_MAX_ATTEMPTS`.

## 2.2 — 2026-05-24

- **Визуал результатов:** сетка SERP-карточек (домен + путь), без случайной раскраски при загрузке; мягкая подсветка по кнопкам.
- Таблица мета-тегов: предупреждение о защищённых сайтах (оранжевый фон вместо «danger»).
- Анализ тегов: чипы `слово · count`, фильтр мусора со скрапа; вложенность — stat-pills.
- Секции в карточках, toolbar `btn-group`, badge v2.2.

## 2.1 — 2026-05-24

- **XMLStock:** `query` в URL через `rawurlencode` (кириллица без кодировки давала ложный **300** «Неизвестная ошибка»).
- Яндекс XMLStock: простой `groupby=10…100` вместо `attr=d.mode=deep…`, если топ входит в допустимые значения.
- Парсинг `<error code="…">` — код ошибки снова попадает в админ-лог (`xml.api_error.code`).

## 2.0 — 2026-05-24

- **XMLStock гибридный режим:** повтор запроса при кодах **210** / **202** (тот же URL, пауза ~22 с, до 8 попыток) — по [документации XMLStock](https://xmlstock.com/?do=help-yaxml-format).
- В админ-лог: `xmlstock.hybrid.attempt`, `xml.api_error` с кодом, `xml.success`.
- Env: `COMPETITOR_XMLSTOCK_HYBRID_RETRY`, `COMPETITOR_XMLSTOCK_HYBRID_SLEEP`, `COMPETITOR_XMLSTOCK_HYBRID_MAX`.
- **Проверка:** 4 фразы Яндекс Москва — в логе попытки 210→повтор; при code **300** — поддержка XMLStock.

## 1.9 — 2026-05-24

- После ошибки/успеха опрос не получает «0%»: кеш `terminal` по `pageHash`, остановка polling на клиенте.
- Toast при `xml.empty_after_phrases` стабильно показывается (раньше строка progress удалялась → ложный сброс).

## 1.8 — 2026-05-24

- **Админ-лог** на `/competitor-analysis`: панель «Расширенный лог прогресса» (admin / Super Admin) — server + browser, опрос, откаты %, job/shutdown.
- Дублирование в `storage/logs/laravel-*.log` с префиксом `[competitor-analysis]`.
- Config: `debug_log`, `CompetitorAnalysisDebugLog`.
- **Проверка:** под админом запустить анализ → под прогресс-баром лог; при сбросе % — строки `progress.skipped_regression` / `ui.regression_blocked`.

## 1.7 — 2026-05-24

- Прогресс **не откатывается** после 15%: парсинг сайтов считается в диапазоне **16–88%** (раньше с 0% → визуальный «сброс»).
- Монотонное обновление % в БД и на фронте; `getProgressBar` при `>= 100`; явный `pageHash` в payload job.
- **Проверка:** 6 фраз → после 15% полоса растёт, не падает к 2%; badge **v1.7**.

## 1.6 — 2026-05-24

- **Скорость:** XML — один запрос на провайдер (xmlstock → xmlproxy → xmlriver), таймаут **12 с** вместо 6×45 с.
- Парсинг сайтов: до **2** попыток curl (было 6×5 с), таймаут **4 с**; прогресс обновляется **на каждый URL**.
- Опрос прогресса раз в **1 с**; % двигается уже во время XML.
- **Проверка:** 6 фраз, топ-10 → до ~15% заметно быстрее; badge **v1.6**.

## 1.5 — 2026-05-24

- **Локально:** анализ запускается сразу после ответа HTTP (`dispatch_now` в `register_shutdown_function`), без `database` очереди и без `exec(php-fpm …)`.
- Старт прогресса с **1%** (раньше 0% до старта job).
- Опционально: `COMPETITOR_ANALYSIS_PHP_CLI`, `spawn_queue_worker` для старого пути через `queue:work --once`.
- **Проверка:** hard refresh → badge **v1.5**; 1–2 фразы, Яндекс → % растёт или toast (XML).

## 1.4 — 2026-05-24

- Прогресс-бар: полоса, этап в `#stage`, опрос 1.5 с; прогресс **1–15%** на этапе XML.
- Локально: анализ в фоне (`queue:work --once`), ответ `analysis.sites` сразу; лог `storage/logs/competitor-analyse-once.log`.
- Ошибка XML/анализа → toast (`failed` в `get-competitor-progress`), без зависания на 1%.
- `SimplifiedXmlFacade`: без рекурсивных retry, таймаут HTTP 45 с, пустая выдача → понятная ошибка.
- `refresh-all.js`: безопасный `DataTable().destroy()` (раньше первый клик «Анализировать» падал).
- **Проверка:** 1–2 фразы, Яндекс, Москва → растёт % или toast про XML; badge **v1.4**.

## 1.3 — 2026-05-24

- За один запуск — **обе ПС** (чекбоксы Яндекс + Google), регионы `regions_yandex[]` / `regions_google[]`.
- Лимит в шапке и баннер на странице (`CompetitorAnalysisLimitsComposer`, `ModuleTariffLimit`).
- Тариф: фразы × сумма регионов по всем ПС; вкладки `Яндекс · Москва` (`byRegion['engine|id']`).
- **Проверка:** Яндекс + Google, по 1 городу, 1 фраза → две вкладки; badge **v1.3**.

## 1.2 — 2026-05-24

- Выбор **поисковой системы**: Яндекс (по умолчанию) или Google.
- Справочник Google: `config/google_geo_regions.json` (города РФ, Criteria ID xmlstock), поиск `GET /competitor-analysis/regions?engine=google&q=…` — **не** `/api/location` мониторинга.
- Пересборка Google: `python3 scripts/build-google-geo-regions.py`.
- **Проверка:** Google + Москва, 1 фраза → выдача через `SimplifiedXmlFacade` (`xmlstock` Google XML).

## 1.1 — 2026-05-24

- До **5 регионов** за один запуск: Select2 multiple, парсинг по каждому городу отдельно.
- Вкладки над результатами для переключения между регионами (если выбрано 2+).
- Лимит тарифа: число фраз × число регионов.
- **Проверка:** выбрать Москва + Санкт-Петербург, 2 фразы, анализ → вкладки, разные топы.

## 1.0 — 2026-05-24

- Введено версионирование модуля (badge в nav).
- Справочник регионов Яндекса (823 `lr`) + поиск Select2 вместо статичного `<select>`.
- Исправлен JS отправки `region` / `count` в анализ.
- **Проверка:** выбор региона «Воронеж», запуск анализа по 1–2 фразам, badge **v1.0** в шапке модуля.
