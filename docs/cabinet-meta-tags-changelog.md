# Changelog: Мониторинг мета-тегов

Модуль: `/meta-tags` · config `cabinet-meta-tags.php` · badge **v1.2.17s**

## 1.2.17s — 2026-05-26

- **Демо API** для datagon.ru: `POST api/demo/proverka-meta-tegov-online/run` — разовая проверка одного URL (title, description, H1, canonical, robots), 5 запусков/сутки, guest cookie.
- **Проверка:** `curl -X POST http://127.0.0.1:3002/api/demo/proverka-meta-tegov-online/run -H 'Content-Type: application/json' -d '{"url":"https://datagon.ru/"}'`

## 1.2.16s — 2026-05-26

- Статистика перенесена в **Администрирование** (`/meta-tags/settings`): KPI-карточки, info-box справа, реестр проектов с DataTable (пользователь, тариф, URL, снимки, ошибки).
- `/meta-tags/statistic` → редирект 301 на `#cabinet-mt-admin-registry`; вкладка «Statistic» убрана из nav.
- Настройки retention — POST-форма с кнопкой «Сохранить».
- **Проверка:** http://localhost:3002/meta-tags/settings · http://localhost:3002/meta-tags/statistic (редирект).

## 1.2.15s — 2026-05-26

- Колонка «Действия»: Bootstrap-tooltip с пояснением «что / зачем» на каждой иконке.
- **Проверка:** http://localhost:3002/meta-tags — наведение на кнопки в таблице проектов.

## 1.2.14s — 2026-05-26

- Сравнение: приглушённые красный/жёлтый (светлый фон бейджа, не «ядерный» `#dc3545`).

## 1.2.13s — 2026-05-26

- Колонка «Действия»: снова только иконки в одной строке, заголовок и кнопки по центру; подсказки при наведении.

## 1.2.12s — 2026-05-26

- Сравнение снимков: **красный** — мета (title, description…), **жёлтый** — h1/h2/h3/ссылки; убран бирюзовый `info`.
- **Проверка:** compare title → красные бейджи; h1 → жёлтые.

## 1.2.11s — 2026-05-26

- Период проекта: только **«Раз в сутки»** (модалка сохранения и таблица проектов).
- **Проверка:** «Сохранить как проект» — без выбора 6/12 ч и «вручную».

## 1.2.10s — 2026-05-26

- Колонка «Действия»: заголовок и кнопки по центру, подписи (История / Запустить / Изменить / Удалить), сетка 2×2.
- **Проверка:** http://localhost:3002/meta-tags — шаг 3, таблица проектов.

## 1.2.9s — 2026-05-26

- **Fix:** сравнение снимков — «Изменилось между снимками» для title (не «Без проблем»): diff_tags после collect, сравнение с peer, на compare скрыт placeholder «Без проблем».
- **Проверка:** compare #9474 / #9472, строка title.

## 1.2.8s — 2026-05-26

- **Сравнение снимков:** при отличии тега — метка «Изменилось между снимками» вместо вводящего в заблуждение «Без проблем».
- **Проверка:** compare двух снимков, строка title с разным текстом.

## 1.2.7s — 2026-05-26

- **Fix:** `/meta-tags/histories/{id}` — OOM при больших снимках (~2 МБ JSON): список без колонки `data`, кэш `errors_count`.
- **Миграция:** `php artisan migrate` → `errors_count` в `meta_tags_histories`.
- **Проверка:** http://localhost:3002/meta-tags/histories/53

## 1.2.6s — 2026-05-26

- Пошаговая навигация как в `/cluster`: в шапке — только номер и название шага; подсказки — в карточке шага.
- Админ-вкладки: «Модуль» / «Администрирование модуля» / «Статистика» (nav-pills, как у кластеризатора); полные тексты шагов — на `/meta-tags/settings`.
- **Проверка:** http://localhost:3002/meta-tags — компактная полоска шагов; текст «Дождитесь окончания загрузки…» под заголовком «Результаты».

## 1.2.5s — 2026-05-26

- **Fix:** «Какие теги снимать» — список с сервера + чекбоксы (не пустой multiselect).
- **Проверка:** http://localhost:3002/meta-tags — title, description, H1… отмечены по умолчанию.

## 1.2.4s — 2026-05-26

- **Сравнение:** фильтр с группами — «Изменилось между снимками» (по тегу: title, description…) и «Ошибки проверки».
- **Проверка:** `/meta-tags/history/{id}/compare/{id2}` — выбрать «Изменился: Title».

## 1.2.3s — 2026-05-26

- **Сравнение снимков** `/meta-tags/history/{id}/compare/{id2}`: рабочий фильтр по типу ошибки и «Только с отличиями»; без оранжевой заливки всей карточки.
- **Проверка:** http://localhost:3002/meta-tags/history/9472/compare/7317 — выбрать title/description в фильтре.

## 1.2.2s — 2026-05-26

- **Текстовый анализ:** из снимка мета-тегов — переход на `/text-analyzer` через `redirect-to-text-analyzer`, автоподстановка URL и автозапуск.
- **Проверка:** `/meta-tags/history/{id}` → меню строки → «Текстовый анализ».

## 1.2.1s — 2026-05-26

- **Fix:** фильтр на `/meta-tags/history/{id}` — после «Показать ещё» и при «Все» список не пропадал; опции пересобираются при подгрузке чанков.
- **Проверка:** http://localhost:3002/meta-tags/history/7317 — выбрать тип ошибки, подгрузить ещё URL.

## 1.2.0s — 2026-05-26

- **Список снимков** `/meta-tags/histories/{id}`: инструкция из 3 шагов, BS5-таблица, кнопка «К мониторингу», сравнение только после выбора второго снимка.
- **Проверка:** http://localhost:3002/meta-tags/histories/{projectId}

## 1.1.0s — 2026-05-26

- **Онбординг:** блок «Как работать с модулем» — 3 шага с подсказками и якорями.
- **Русский интерфейс:** подписи полей, периоды, тосты, названия тегов; пустой список проектов с подсказкой.
- **Проверка:** http://localhost:3002/meta-tags

## 1.0.0s — 2026-05-26

- **Список (AdminLTE 4):** карточки проверки URL и проектов, BS5 form-switch, `btn-group` + Bootstrap Icons.
- **Админ:** навигация Settings/Statistic с `bi-*`; отдельный CSS `cabinet-meta-tags.css`.
- **Модалки:** `btn-close`, `form-check` / `form-select` (BS5).
- **Проверка:** http://localhost:3002/meta-tags
