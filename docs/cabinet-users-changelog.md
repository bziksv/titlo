# Changelog: Пользователи (админ)

`/users` · `config/cabinet-users.php` · **v1.2.6s**

## 1.2.6s — 2026-06-01

- **Зависшие расписания** снова на `/users` (общий блок с `/monitoring/admin`, partial + `cabinet-monitoring-stale-schedules.js`).
- **Проверка:** http://localhost:3002/users — карточка «Мониторинг: зависшие расписания»; фильтр и таблица пользователей.

## 1.2.5s — 2026-06-01

- Блок зависших расписаний временно только на `/monitoring/admin` (см. 1.2.6s — возврат на `/users`).

## 1.2.4s

## 1.2.4s — 2026-05-30

- **Пересчитать объём для всех:** полоска прогресса с % и «Обработано N из M · осталось K»; toast при повторном клике; таблица обновляется реже (без белого спиннера на каждой пачке).
- **Проверка:** http://localhost:3002/users — кнопка «Пересчитать объём для всех» → карточка прогресса под подсказкой.

## 1.2.3s — 2026-05-30

- **Данные в БД:** полный пересчёт идёт **с больших id вниз** (как в таблице по умолчанию) — видимые пользователи заполняются первыми; пачка 15 вместо 10.
- Сначала пересчитывается **текущая страница**, затем все остальные; повторный клик во время прогона игнорируется; авто-дозаполнение строки не конфликтует с полным прогоном.
- **«не рассчитано»** — нет кэша; **«0»** — пересчитано, данных в модулях нет (новый Free без проектов).
- **Проверка:** http://localhost:3002/users — после загрузки или одного «Пересчитать объём для всех» колонка показывает `0` или объём, не «не рассчитано».

## 1.2.2s — 2026-05-30

- Сортировка по **всем** столбцам данных, включая «Данные в БД» (по кэшу footprint); «Действия» без сортировки.
- Иконки сортировки в thead — у правого края заголовка, без «разъезда».
- **Проверка:** http://localhost:3002/users — клик по каждому заголовку (кроме «Действия»).

## 1.2.1s — 2026-05-30

- Блок «Управление рассылками» убран с `/users` → отдельная страница `/admin/notifications`.

## 1.2.0s — 2026-05-30

- *(перенесено на `/admin/notifications` в v1.0.0s `cabinet-notifications-admin`)*

## 1.1.9s — 2026-05-30

- Таблица: убран «обрыв» перед колонкой «Действия» — `display:flex` больше не на `<td>`, только на внутреннем блоке кнопок.
- **Проверка:** http://localhost:3002/users — полосы строк и границы идут через всю таблицу.

## 1.1.8s — 2026-05-30

- **Данные в БД:** полный пересчёт для ~2500 пользователей больше не уходит в один HTTP-запрос (таймаут). Пачки по 10, прогресс на кнопке; для текущей страницы таблицы — авто-пересчёт строк без кэша.
- Ускорен подсчёт: кэш `Schema::hasTable` на время запроса.
- **Проверка:** http://localhost:3002/users — колонка «Данные в БД» заполняется (0 или объём) после загрузки страницы или кнопки «Пересчитать объём для всех».

## 1.1.7s — 2026-05-30

- Селектор «строк на странице» (10/25/50/100) — справа над таблицей.
- **Проверка:** http://localhost:3002/users — выпадающий список у правого края, над колонкой «Действия».

## 1.1.6s — 2026-05-30

- «Выгрузка и действия» — в `card-tools` справа в строке «Список пользователей» (эталон AdminLTE).
- **Проверка:** http://localhost:3002/users — кнопка у правого края заголовка карточки.

## 1.1.5s — 2026-05-30

- Убраны фильтры во второй строке заголовка таблицы и блок «Пояснения к таблице».
- Порядок после KPI: **Фильтры** → **Зависшие расписания** → **Список пользователей**; график активности — внизу страницы.
- **Проверка:** http://localhost:3002/users — без строки фильтров в thead и без legend.

## 1.1.4s — 2026-05-30

- **Фильтры по колонкам** в таблице пользователей: ID (от–до), имя, email + верификация, тариф (в т.ч. Free / без роли), даты регистрации, роль, «Был в сети». Синхронизация с блоком «Фильтры».
- **Проверка:** http://localhost:3002/users → вторая строка заголовка таблицы; тариф «Free» или «Без тарифной роли».

## 1.1.3s — 2026-05-30

- **Колонка «Тариф»:** показывается по **роли** (Ultimate, Free, …), даже если нет активной записи в `pays`; подпись «По роли, без активной подписки».
- **Проверка:** http://localhost:3002/users — пользователь с ролью Ultimate без pays → badge «Ultimate», не «—».

## 1.1.2s — 2026-05-30

- **Список пользователей:** серверная сортировка по **Тариф** (роль тарифа: Optimal / Ultimate / Maximum / Free) и **Роли**; «Данные в БД» и «Действия» без сортировки. Имя — по полному ФИО.
- **Проверка:** http://localhost:3002/users → клик по заголовку «Тариф».

## 1.1.1s — 2026-05-30

- **Зависшие расписания:** сортировка по колонкам (серверная, DataTables) — домен, email, «Был в сети», тариф, слова, расписание (по числу регионов auto); «Действия» без сортировки. По умолчанию — по убыванию числа слов.
- **Проверка:** http://localhost:3002/users → раскрыть блок → клик по заголовку «Слова» / «Был в сети».

## 1.1.0s — 2026-05-30

- **Зависшие расписания мониторинга:** блок на `/users` — проекты с `auto_update` у владельцев без входа > N дней (по умолчанию 90, `CABINET_USERS_STALE_MONITORING_DAYS`). KPI, таблица (домен, email, расписание, ключи), фильтр списка пользователей, отключение auto_update по проекту или по всем проектам пользователя.
- **Объём данных в БД:** оценка строк / ~MB по модулям (кэш на пользователя). Колонка в списке, деталка на `/users/{id}/edit`, кнопка «Пересчитать объём для всех», команда `php artisan users:storage-footprint`.
- **Проверка:** http://localhost:3002/users → раскрыть «Мониторинг: зависшие расписания»; фильтр «Есть проекты с auto_update»; `php artisan users:storage-footprint --user=1`.

## 1.0.0s — 2026-05-26

- Badge версии в шапке админ-страницы пользователей.
- **Проверка:** http://localhost:3002/users
